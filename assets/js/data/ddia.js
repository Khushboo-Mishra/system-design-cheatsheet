/* Concepts from "Designing Data-Intensive Applications" (Kleppmann), plus the chapter → concept map.
 * All explanations are original summaries written for this cheat sheet. */
window.SD = window.SD || {};
(function () {
  const n = (id, label, sub, type, x, y, desc, icon) => ({ id, label, sub, type, x, y, desc, icon });
  const C = (id) => SD.concepts.find((c) => c.id === id);

  SD.conceptCategories = ["Fundamentals", "Data Models & Encoding", "Networking & Traffic", "Caching", "Data & Storage", "Transactions", "Messaging & Streaming", "Batch Processing", "Distributed Systems", "Faults & Time", "Reliability & Resilience", "Derived Data & Correctness", "Architecture Patterns", "Security", "Observability", "Algorithms & Structures", "Object-Oriented Design"];

  /* ------------------------- patches to existing concepts ------------------------- */
  C("isolation-levels").cat = "Transactions";
  C("isolation-levels").anim = "isolation";
  C("acid-base").cat = "Transactions";
  C("acid-base").points.push(
    "The 'C' in ACID is mostly the application's job: the database only enforces the invariants you declare (constraints). Atomicity, isolation and durability are properties the DB provides.",
    "Single-object operations (atomic increment, compare-and-set, one-row writes with a WAL) are atomic even in most NoSQL stores. Multi-object transactions matter for foreign keys, denormalised copies and secondary indexes staying in sync.",
    "Aborting is a feature: a failed transaction can be retried safely. Only retry transient errors, beware retries after a lost commit acknowledgement, and make side effects idempotent."
  );
  C("storage-engines").points.push(
    "The simplest index is a log-structured hash index: an append-only data file plus an in-memory hash map of key → byte offset (Bitcask). It's very fast, but all keys must fit in RAM and range scans are impossible.",
    "SSTables keep each segment sorted by key, so merging segments is a merge-sort, the in-memory index can be sparse, and blocks can be compressed. Compaction strategies are size-tiered (Cassandra default) or leveled (LevelDB/RocksDB).",
    "B-trees split data into fixed-size pages (~4 KB) with a high branching factor, so a 4-level tree can address hundreds of TB. Updates overwrite pages in place and rely on a WAL for crash safety, plus latches for concurrency.",
    "Trade-offs: LSM-trees usually have lower write amplification and better compression, but compaction can interfere with foreground I/O. B-trees keep each key in exactly one place, which makes transactional locking simpler and latency more predictable."
  );
  C("indexing").points.push(
    "Where values live: a clustered index stores the row inside the index (InnoDB primary key); a non-clustered index points to a heap file; a covering index stores some extra columns to answer queries index-only.",
    "Multi-dimensional queries (lat AND lng, or date range AND temperature) need R-trees or space-filling curves; a plain concatenated index only helps its leftmost columns.",
    "Full-text and fuzzy indexes (Lucene) keep a term dictionary as a finite-state automaton, which enables search within an edit distance.",
    "In-memory databases (VoltDB, Redis, MemSQL) are fast mostly because they avoid encoding data into disk-friendly structures, not just because they skip disk reads. Anti-caching can spill the least recently used data to disk."
  );
  C("two-phase-commit").points.push(
    "Database-internal distributed transactions (within one system such as Spanner or VoltDB) can work well. Heterogeneous ones (XA across a DB and a message broker) are fragile.",
    "The costly failure case: a participant that voted YES is 'in doubt' and must hold its locks until the coordinator recovers. Orphaned in-doubt transactions need manual or heuristic resolution, which breaks atomicity.",
    "2PC is an atomic commit protocol, not fault-tolerant consensus: it blocks if the coordinator dies. 3PC assumes bounded network delay, which real networks don't provide."
  );
  C("consensus").points.push(
    "Formal properties: uniform agreement (no two nodes decide differently), integrity (decide at most once), validity (the decided value was proposed), termination (every non-crashed node eventually decides).",
    "FLP result: no deterministic algorithm can always reach consensus in a purely asynchronous system if a node may crash. Real systems escape it with timeouts and randomisation.",
    "Protocols use epoch/term numbers: each epoch has a unique leader, and a leader must collect a quorum vote before deciding, so an old leader from an earlier epoch can't override.",
    "Costs: synchronous replication to a majority, a fixed membership that is awkward to change, sensitivity to network timeouts (frequent re-elections), and a strict majority must be up."
  );
  C("queues-pubsub").points.push(
    "Two questions define a messaging system: what happens when producers outpace consumers (drop, buffer, or apply backpressure), and whether messages survive node crashes (in memory vs replicated on disk).",
    "Direct messaging (UDP multicast, brokerless ZeroMQ, webhooks) has low latency, but messages are lost if a party is offline.",
    "Traditional brokers delete messages after acknowledgement. With load-balanced consumers, redelivery after a crash can reorder messages.",
    "Log-based brokers (Kafka) keep an offset per consumer group, like a replication log position. Consumer lag is visible, old messages can be replayed, and the log is a bounded circular buffer on disk."
  );
  C("batch-stream").points.push("Stream processing uses: complex event processing (standing queries that search for patterns), stream analytics (rates, rolling averages), keeping materialised views and caches up to date, and search on streams (stored queries matched against incoming documents).");
  C("consistency-models").points.push("Weak consistency: after a write, reads may or may not see it at all (best-effort caches, VoIP, real-time games). Eventual consistency guarantees convergence; strong consistency means reads see the latest write.");

  /* ---------------------------------- new concepts ---------------------------------- */
  const add = (c) => SD.concepts.push(c);

  /* Chapter 1 */
  add({ id: "reliability", cat: "Fundamentals", title: "Reliability: Faults vs Failures",
    summary: "A reliable system keeps doing the right thing even when parts go wrong. The goal is fault tolerance: stop faults from turning into failures.",
    points: ["Modern apps are data systems built from building blocks (DB, cache, search index, queue, stream processor) glued together by application code, and you are the designer of that composite system and its guarantees.", "Fault = one component deviating from its spec. Failure = the whole system stops serving users. You can't prevent all faults, so design so they don't cascade.", "Hardware faults: disks, RAM, power and network. With thousands of disks, one dies every day or so. Mitigate with redundancy, and prefer software that tolerates losing entire machines (which also enables rolling upgrades).", "Software faults are correlated and systematic: a bug that hits every node on the same input, a runaway process, a slow dependency, cascading failures. Mitigate with process isolation, crash-and-restart, careful assumptions, testing and monitoring of invariants.", "Human error (especially configuration changes) causes a large share of outages. Mitigate with good abstractions, sandboxes, quick rollback, gradual rollouts, detailed telemetry and training.", "Deliberately injecting faults (chaos engineering) keeps the recovery paths exercised."],
    use: ["Frame NFR discussions: which faults do we tolerate, and how?"], pitfalls: ["Assuming hardware faults are independent (a whole rack or AZ can fail together)", "Treating 'it's only a prototype' as a reason to skip reliability for user data"] });
  add({ id: "load-parameters", cat: "Fundamentals", title: "Describing Load & Performance",
    summary: "Before discussing scale, name the load parameters that matter for your system, then describe performance as a distribution, not an average.",
    points: ["Load parameters: requests/s, read/write ratio, concurrent users, cache hit rate, and fan-out (how many downstream operations one request triggers).", "Classic example: a social home timeline. Posting is rare but reading timelines is frequent, so precompute timelines on write (fan-out), except for accounts with huge follower counts (hybrid). The key load parameter is the distribution of followers per user.", "Batch systems care about throughput; online systems care about response time. Response time is what the client sees (service time + queueing + network); latency is time spent waiting to be served.", "Use percentiles: the median shows the typical case, p95/p99/p999 show the tail. Tail users are often the most valuable (they have the most data).", "Head-of-line blocking: a few slow requests hold up the queue behind them. Measure response times on the client side, and use realistic load generators that don't wait for responses.", "Tail latency amplification: when one user request fans out to many backends, the slowest backend decides the response time."],
    use: ["Justify architecture choices with concrete load parameters"], pitfalls: ["Averaging percentiles across servers (merge histograms instead)", "Load tests whose clients slow down when the server does"] });
  add({ id: "maintainability", cat: "Fundamentals", title: "Maintainability: Operability, Simplicity, Evolvability",
    summary: "Most of a system's cost is in ongoing maintenance. Design for the people who will run, understand and change it.",
    points: ["Operability: make routine tasks easy. That means good monitoring, automation hooks, no dependency on individual machines, documentation, sensible defaults with overrides, self-healing where safe, and predictable behaviour.", "Simplicity: remove accidental complexity (complexity from implementation, not from the problem). Good abstractions such as SQL, high-level languages and clean APIs hide detail and are reusable.", "Evolvability: requirements always change. Simple, well-abstracted systems are easier to modify. Agile practices (TDD, refactoring) apply at the architecture level too.", "Symptoms of a 'big ball of mud': tight coupling, tangled dependencies, inconsistent naming, special cases and performance hacks."],
    use: ["Wrap-up discussions: how will this be operated and evolved?"], pitfalls: ["Clever designs that only their author understands"] });

  /* Chapter 2 */
  add({ id: "data-models", cat: "Data Models & Encoding", title: "Relational vs Document vs Graph Models",
    summary: "The data model shapes how you think about the problem. Pick the model that fits your relationships: trees favour documents, many-to-many favours relational or graph.",
    points: ["Relational: data in relations (tables) of tuples. The query optimiser picks the access path, so new queries don't need new code paths.", "Document (JSON): a self-contained tree per record. Good locality (one read loads everything) and a closer fit to application objects, which avoids the object-relational impedance mismatch.", "One-to-many (a résumé with its jobs) fits a document tree. Many-to-one (many users in one region) and many-to-many need references by ID and joins, where documents are weak and joins move into application code.", "Normalise with IDs so human-meaningful data is stored once (no update anomalies).", "Schema-on-read (documents: structure is implicit, interpreted when read) vs schema-on-write (relational: enforced up front). It's like dynamic vs static typing. Schema-on-read helps with heterogeneous or externally defined data.", "History rhymes: the 1970s hierarchical (IMS) and network (CODASYL) models made programmers navigate access paths by hand; the relational model won by letting the optimiser do it. Today relational DBs support JSON and document DBs support joins, so the models are converging."],
    use: ["Documents for self-contained aggregates; relational when relationships dominate; graph when anything can relate to anything"], pitfalls: ["Choosing a document DB, then emulating joins in app code everywhere", "Large documents rewritten entirely on small updates"],
    table: [["", "Relational", "Document", "Graph"], ["Best shape", "Tables + many-to-many", "Trees / aggregates", "Highly interconnected"], ["Joins", "Native", "Weak / app-side", "Traversals are the point"], ["Schema", "On write", "On read (flexible)", "Flexible"], ["Locality", "Spread across tables", "Whole doc in one place", "Adjacency"], ["Examples", "PostgreSQL, MySQL", "MongoDB, Couchbase", "Neo4j, Neptune"]] });
  add({ id: "graph-models", cat: "Data Models & Encoding", title: "Graph Data Models & Query Languages",
    summary: "When anything can relate to anything, model vertices and edges explicitly and query them with graph languages.",
    points: ["Property graph: vertices and edges each have an ID, a label and key-value properties. Edges have a tail and a head vertex, so you can traverse both ways. Queried with Cypher (Neo4j) or Gremlin.", "Triple-store: everything is (subject, predicate, object). Objects are either literals or other vertices. RDF and the semantic web use this, queried with SPARQL.", "Datalog: facts plus rules that derive new facts recursively. It's the foundation many graph query languages build on (Datomic, Cascalog).", "In SQL, variable-length traversals need recursive common table expressions (WITH RECURSIVE), which is possible but verbose compared with a single Cypher pattern.", "Graphs evolve easily: different kinds of vertices and relationships coexist in one store without schema changes."],
    use: ["Social graphs, fraud rings, knowledge graphs, network topology, recommendation"], pitfalls: ["Using a graph DB for mostly tabular aggregate queries"] });
  add({ id: "query-languages", cat: "Data Models & Encoding", title: "Declarative vs Imperative Queries",
    summary: "Declarative languages say what you want, not how to get it. That lets the engine optimise and parallelise.",
    points: ["Imperative code fixes an execution order: loops, and which index to use.", "Declarative queries (SQL, CSS selectors) specify the pattern of results. The engine can pick indexes and join orders, change its internals without breaking queries, and run in parallel.", "MapReduce querying sits in between: you write map and reduce functions, which must be pure, and the framework distributes them. MongoDB added a declarative aggregation pipeline on top for usability.", "Graph languages (Cypher, SPARQL, Datalog) are declarative too: describe the pattern and let the engine find matches."],
    use: ["Prefer declarative interfaces for data access; they age better"], pitfalls: ["Hand-optimised imperative data access that blocks future engine improvements"] });

  /* Chapter 3 */
  add({ id: "oltp-olap", cat: "Data & Storage", title: "OLTP vs OLAP & Data Warehousing",
    summary: "Transaction processing and analytics have opposite access patterns, so they get separate systems: an OLTP database and a data warehouse.",
    points: ["OLTP: many small reads and writes by key, random access, user-facing, latest state, GBs to TBs.", "OLAP: scans and aggregates over millions of rows, bulk loads or streams, used by analysts and BI tools, full history, TBs to PBs.", "A data warehouse is a separate, read-optimised copy of data from all the OLTP systems, loaded via ETL/ELT or CDC. Analysts can query freely without hurting production latency.", "Star schema: a central fact table (one row per event, like a sale, often hundreds of columns wide) with foreign keys into dimension tables (who, what, where, when, how). A snowflake schema normalises dimensions further into sub-dimensions.", "Warehouses expose SQL, but internally they are column stores optimised for scans."],
    use: ["Any time business users need reporting over production data"], pitfalls: ["Running heavy analytical queries on the primary OLTP database"],
    table: [["", "OLTP", "OLAP"], ["Read pattern", "Few rows by key", "Aggregate over many rows"], ["Write pattern", "Random, low latency", "Bulk load / stream"], ["Users", "End users via apps", "Analysts, BI, data science"], ["Data", "Current state", "History of events"], ["Size", "GB – TB", "TB – PB"]] });
  add({ id: "column-storage", cat: "Data & Storage", title: "Column-Oriented Storage",
    summary: "Store each column's values together, so analytic queries read only the few columns they need, and those columns compress extremely well.",
    points: ["Row stores lay out whole rows together; a query touching 4 of 100 columns still reads all 100. Column stores keep one file per column, with rows aligned by position.", "Compression: columns have few distinct values, so bitmap encoding plus run-length encoding shrinks them dramatically. Bitmaps can also be ANDed and ORed directly to evaluate WHERE clauses.", "Vectorised processing: tight loops over compressed column chunks that fit in CPU cache, using SIMD instructions.", "Sort order: sort rows by commonly filtered columns (such as date, then product). This speeds up range filters and makes the first sort column compress into long runs. Replicas can even use different sort orders.", "Writes: in-place updates are hard. Buffer writes in an in-memory row store (LSM-style), then merge into column files in bulk.", "Materialised aggregates: materialised views and data cubes precompute aggregates along dimensions, making queries instant but rigid."],
    use: ["Warehouses (Redshift, BigQuery, Snowflake), real-time OLAP (ClickHouse, Druid, Pinot), Parquet/ORC files"], pitfalls: ["Point lookups and frequent single-row updates on a column store"] });

  /* Chapter 4 */
  add({ id: "encoding-formats", cat: "Data Models & Encoding", anim: "encoding", title: "Encoding & Schema Evolution",
    summary: "Data outlives code. The encoding format decides whether old and new versions of your software can read each other's data during rolling upgrades.",
    points: ["Encoding (serialisation) turns in-memory objects into bytes for files and networks; decoding reverses it.", "Language-built-in formats (Java Serializable, Python pickle) tie you to one language, are slow, version poorly, and can be a security hole (decoding can instantiate arbitrary classes).", "JSON/XML/CSV are human-readable and widespread, but number handling is ambiguous (JSON loses integers above 2^53), there are no binary strings (Base64 bloats them), and schemas are optional and rarely used.", "Thrift and Protocol Buffers: a schema with numbered field tags gives compact binary encoding. Add new fields with new tags (optional or with defaults); never reuse a deleted tag.", "Avro: no tags in the data. The reader resolves the writer's schema against its own reader's schema by field name. That suits dynamically generated schemas (such as DB dumps). Ship the writer's schema in the file header or a schema registry.", "Backward compatibility: new code reads old data. Forward compatibility: old code reads new data (it must ignore unknown fields, and must not drop them when rewriting a record).", "Schemas are valuable documentation, enable compatibility checks before deploys, and allow code generation in statically typed languages."],
    use: ["Protobuf/gRPC for services, Avro for Kafka + a schema registry, JSON for public APIs"], pitfalls: ["Adding a required field (breaks reading old data)", "Old code decoding, modifying and re-encoding a record, silently dropping new fields"],
    table: [["Format", "Schema", "Field identity", "Size", "Evolution"], ["JSON / XML", "Optional", "Field names in every record", "Large", "Ad hoc"], ["MessagePack / BSON", "None", "Field names", "Medium", "Ad hoc"], ["Protobuf / Thrift", "Required (IDL)", "Tag numbers", "Small", "Add/remove optional fields by tag"], ["Avro", "Required (JSON/IDL)", "Name, resolved writer ↔ reader", "Smallest", "Defaults; writer schema needed"]] });
  add({ id: "dataflow-modes", cat: "Data Models & Encoding", title: "Modes of Dataflow: Databases, RPC & Messages",
    summary: "Encoded data flows between processes in three ways, and each has its own compatibility and failure concerns.",
    points: ["Through databases: the writer encodes and a reader (maybe a future version of the code) decodes later. Mixed versions write the same rows, and old data persists for years unless migrated.", "Through services (REST/RPC): RPC tries to make a remote call look local, but network calls can time out, be retried (so they must be idempotent), vary in latency, and must encode every argument. Location transparency is a leaky abstraction.", "For service evolution, servers usually update before clients, so requests need backward compatibility and responses need forward compatibility. Version public APIs (URL, header, per-client setting).", "Through asynchronous messages (brokers): the broker buffers when consumers are down, redelivers after crashes, decouples sender from recipient, and fans out to many consumers.", "Actor frameworks (Akka, Orleans, Erlang/OTP) send messages between actors across nodes; rolling upgrades need compatible message encoding."],
    use: ["Pick RPC for request/response inside a service boundary; messages for decoupling and buffering"], pitfalls: ["Treating a remote call like a local function call (no timeouts, blind retries)"] });

  /* Chapter 5 */
  add({ id: "replication-logs", cat: "Data & Storage", title: "Replication Logs, New Followers & Failover",
    summary: "How changes get from leader to followers, how a new replica joins, and what goes wrong during failover.",
    points: ["Statement-based replication ships SQL statements. It breaks with nondeterminism (NOW(), RAND()), auto-increments and side-effecting triggers.", "WAL shipping sends the storage engine's low-level log. It's exact, but coupled to the engine version, which complicates zero-downtime upgrades.", "Logical (row-based) logs describe inserted, updated and deleted rows independently of storage format. This allows version mismatches and is ideal for change data capture (e.g. the MySQL row binlog).", "Trigger-based replication (application-level) is flexible but has more overhead and more bugs.", "Adding a follower without downtime: take a consistent snapshot, copy it, then request every change since the snapshot's exact log position (LSN / binlog coordinates) until caught up.", "Follower crash: catch up from its local log. Leader crash (failover): detect by timeout, elect the most up-to-date follower, reconfigure clients and old leader. Risks: losing unreplicated async writes, reused auto-increment IDs leaking data to other systems, split brain, and timeouts that are too eager or too lax."],
    use: ["Understanding CDC tools, choosing sync vs async, planning failover"], pitfalls: ["Automatic failover with a short timeout during load spikes (flapping)", "Other systems keyed on IDs that the new leader reissues"] });
  add({ id: "replication-lag", cat: "Data & Storage", anim: "lag", title: "Replication Lag: Read-Your-Writes, Monotonic & Consistent Prefix Reads",
    summary: "Asynchronous followers are eventually consistent, and the lag can be seconds or minutes. Three weaker-than-strong guarantees prevent the most confusing anomalies.",
    points: ["Read-your-writes (read-after-write): users always see their own updates. Read things the user may have modified from the leader, or for a while after they write; or track the timestamp of their last write and only read from replicas that have caught up. Cross-device needs a centralised timestamp.", "Monotonic reads: a user never sees data go backwards in time after seeing newer data. Route each user to the same replica (e.g. by hashing the user ID).", "Consistent prefix reads: if writes happen in a causal order, readers see them in that order. This is a problem in partitioned DBs where partitions replicate independently. Keep causally related writes in the same partition, or track causal dependencies.", "Design question: if lag grew to minutes, would the app behave badly? If so, you need a stronger guarantee than 'eventual'. Transactions exist so developers don't have to reason about this."],
    use: ["Read-heavy systems with async replicas and user-visible writes"], pitfalls: ["Load-balancing reads across replicas right after a user's write"] });
  add({ id: "multi-leader", cat: "Data & Storage", title: "Multi-Leader Replication & Conflict Resolution",
    summary: "Several nodes accept writes and replicate to each other asynchronously. This is great for multi-datacenter and offline use, but concurrent writes can conflict.",
    points: ["Use cases: one leader per datacenter (local write latency, survives DC outages), offline-capable clients (each device is a leader, e.g. calendars, CouchDB), and real-time collaborative editing.", "Conflicts are detected asynchronously, often after both writes succeeded, so you can't just ask the user to retry.", "Avoidance: route all writes for a record to one 'home' leader. This is the simplest and most common approach.", "Convergence strategies: last-write-wins (by timestamp or replica ID; silently discards data), merge the values, record the conflict as siblings and resolve on read or in app code, or use CRDTs and operational transformation for automatic merges.", "Topologies: circular, star, or all-to-all. Circular and star have single points of failure; all-to-all can deliver writes out of causal order (an update before its insert), so use version vectors.", "Auto-increment keys, uniqueness constraints and triggers are dangerous with multiple leaders."],
    use: ["Multi-region active-active, offline-first apps, collaborative tools"], pitfalls: ["LWW losing concurrent writes silently", "Assuming ordering across leaders"],
    diagram: { nodes: [
      n("u1", "User in EU", "", "client", 80, 120, "Writes to the nearest datacenter."),
      n("u2", "User in US", "", "client", 80, 400, "Writes concurrently to the other datacenter."),
      n("l1", "Leader · EU DC", "accepts writes", "db", 340, 120, "Applies writes locally, then replicates asynchronously."),
      n("l2", "Leader · US DC", "accepts writes", "db", 340, 400, "Applies writes locally, then replicates asynchronously."),
      n("res", "Conflict handler", "LWW / merge / CRDT", "worker", 620, 260, "Detects concurrent writes to the same record and converges them."),
      n("f1", "EU followers", "", "db", 880, 120, "Read replicas in the EU."),
      n("f2", "US followers", "", "db", 880, 400, "Read replicas in the US.")
    ], edges: [["u1", "l1"], ["u2", "l2"], ["l1", "l2", "", 0.18], ["l2", "l1", "", 0.18], ["l1", "res"], ["l2", "res"], ["l1", "f1"], ["l2", "f2"]], flows: [{ name: "Concurrent edits", steps: [
      ["u1", "l1", "UPDATE doc SET title = 'B' (applied locally, fast)"],
      ["u2", "l2", "UPDATE doc SET title = 'C' at the same time"],
      ["l1", "l2", "Async replication: title = 'B' arrives in the US"],
      ["l2", "l1", "title = 'C' arrives in the EU, so both leaders detect a conflict"],
      ["l1", "res", "Resolve: LWW keeps the higher timestamp (other write silently lost) …"],
      ["l2", "res", "… or keep both as siblings and merge, or use a CRDT"],
      ["l1", "f1", "Every replica must converge to the same final value"],
      ["l2", "f2", "Convergence achieved"]
    ]}]} });
  add({ id: "concurrent-writes", cat: "Distributed Systems", title: "Detecting Concurrent Writes: Happens-Before & Version Vectors",
    summary: "Two writes are concurrent if neither knew about the other, regardless of clock time. Version numbers let a database keep both instead of silently dropping one.",
    points: ["A happens-before B if B knew about A, depended on it, or built on it. Otherwise they're concurrent, and there's no 'right' order to pick.", "Last-write-wins forces an arbitrary order and discards concurrent writes. It's acceptable only for immutable keys (e.g. UUID-keyed inserts).", "Single replica: the server keeps a version number per key. The client must read before writing and send back the version it saw. The server overwrites values up to that version and keeps concurrent ones as siblings.", "Clients merge siblings (e.g. union of shopping cart items). Deletions need tombstones, or removed items reappear. CRDTs automate the merge.", "Multiple replicas: a version vector (one counter per replica per key) captures what each write had seen. Riak uses dotted version vectors."],
    use: ["Leaderless and multi-leader stores, offline sync"], pitfalls: ["Union merges that resurrect deleted items"],
    diagram: { nodes: [
      n("c1", "Client 1", "cart", "client", 100, 110, "Adds milk, later flour."),
      n("c2", "Client 2", "cart", "client", 100, 410, "Adds eggs, later ham."),
      n("db", "Database", "versioned key 'cart'", "db", 520, 260, "Keeps concurrent values as siblings, tagged with versions.")
    ], edges: [["c1", "db"], ["c2", "db"]], flows: [{ name: "Shopping cart siblings", steps: [
      ["c1", "db", "set cart = [milk] (no prior version)"],
      ["db", "c1", "v1 → [milk]"],
      ["c2", "db", "set cart = [eggs] (hasn't seen v1)"],
      ["db", "c2", "Concurrent! v2 → siblings [milk] and [eggs]"],
      ["c1", "db", "set [milk, flour] based on v1"],
      ["db", "c1", "v3 → [milk, flour] and [eggs] (v1 overwritten, eggs kept)"],
      ["c2", "db", "Merged siblings: set [eggs, milk, ham] based on v2"],
      ["db", "c2", "v4 → [milk, flour] and [eggs, milk, ham]. The next read merges them to [milk, flour, eggs, ham]"]
    ]}]} });

  /* Chapter 6 */
  add({ id: "secondary-index-partitioning", cat: "Data & Storage", title: "Partitioning Secondary Indexes: Local vs Global",
    summary: "Secondary indexes don't map neatly to partitions. You either index each partition locally (cheap writes, scatter reads) or partition the index by term (cheap reads, expensive writes).",
    points: ["Document-partitioned (local) index: each partition indexes only its own documents. Writes touch one partition, but a query by secondary attribute must scatter to all partitions and gather the results, which suffers from tail latency. Used by MongoDB, Cassandra, Elasticsearch, Solr.", "Term-partitioned (global) index: the index itself is partitioned by the indexed value (e.g. color:red lives on one partition). Reads go to a single partition, but a single document write may update several index partitions.", "Global index updates are usually asynchronous (DynamoDB global secondary indexes are eventually consistent), because a synchronous version needs a distributed transaction.", "Range-partitioning the index by term enables range scans; hashing the term spreads load evenly."],
    use: ["Choose local for write-heavy workloads, global for read-heavy lookups by secondary attribute"], pitfalls: ["Assuming a global index is immediately consistent with its table"],
    table: [["", "Local (by document)", "Global (by term)"], ["Write cost", "One partition", "Several partitions"], ["Read cost", "Scatter/gather to all", "One partition"], ["Consistency", "Synchronous with data", "Usually async"], ["Examples", "MongoDB, Cassandra, ES", "DynamoDB GSI, Riak search"]] });
  add({ id: "rebalancing", cat: "Data & Storage", title: "Rebalancing Partitions",
    summary: "When nodes are added or removed, partitions must move. Do it fairly, while serving traffic, and moving as little data as possible.",
    points: ["Don't use hash mod N: changing N moves almost every key.", "Fixed number of partitions: create many more partitions than nodes (e.g. 1,000 for 10 nodes) and move whole partitions between nodes. Simple, but choosing the count up front is hard (Riak, Elasticsearch, Couchbase).", "Dynamic partitioning: split a partition when it grows past a size and merge small ones. Adapts to data volume. Pre-split an empty DB to avoid a single hot partition (HBase, MongoDB).", "Partitions proportional to nodes: a fixed number of partitions per node, and a new node splits random existing partitions (Cassandra vnodes).", "Automatic vs manual: fully automatic rebalancing combined with automatic failure detection can cascade (an overloaded node looks dead, so load moves and makes it worse). Many operators keep a human in the loop."],
    use: ["Scaling a partitioned datastore up or down"], pitfalls: ["Rebalancing during peak traffic", "Too few fixed partitions to grow into"] });
  add({ id: "request-routing", cat: "Data & Storage", title: "Request Routing & Parallel Query",
    summary: "Once data is partitioned, something has to know which node holds which key. That's service discovery for partitions.",
    points: ["Three designs: a client contacts any node, which forwards if needed (gossip-aware nodes); a partition-aware routing tier; or a partition-aware client.", "The mapping must be agreed on. Many systems keep it in a coordination service (ZooKeeper, etcd) that nodes register with and routers watch (HBase, SolrCloud, Kafka before KRaft).", "Others gossip cluster state between nodes (Cassandra, Riak), or use a config server (MongoDB).", "MPP (massively parallel processing) analytic databases go further: the optimiser splits one complex query into stages executed in parallel across many partitions."],
    use: ["Any sharded datastore or sharded service"], pitfalls: ["Clients caching stale partition maps without a refresh or redirect mechanism"],
    diagram: { nodes: [
      n("client", "Client", "", "client", 80, 260, "Doesn't know where data lives."),
      n("router", "Routing tier", "partition-aware", "edge", 300, 260, "Keeps a cached copy of the partition map.", "lb"),
      n("zk", "ZooKeeper", "partition → node map", "db", 300, 70, "The authoritative assignment, maintained with consensus."),
      n("n1", "Node 1", "partitions 0–3", "db", 600, 100, "Owns some partitions."),
      n("n2", "Node 2", "partitions 4–7", "db", 600, 260, "Owns some partitions."),
      n("n3", "Node 3", "partitions 8–11", "db", 600, 420, "Owns some partitions.")
    ], edges: [["client", "router"], ["router", "zk"], ["n1", "zk"], ["n2", "zk"], ["n3", "zk"], ["router", "n2"]], flows: [{ name: "Route a request", steps: [
      ["n1", "zk", "Register: I own partitions 0–3"], ["n2", "zk", "Register: 4–7"], ["n3", "zk", "Register: 8–11"],
      ["router", "zk", "Subscribe to changes in the partition map"],
      ["client", "router", "GET user:42"],
      ["router", "n2", "hash(user:42) → partition 5 → Node 2"],
      ["n2", "client", "Value returned"]
    ]}]} });

  /* Chapter 7 */
  add({ id: "lost-updates-write-skew", cat: "Transactions", anim: "isolation", title: "Race Conditions: Lost Updates, Write Skew & Phantoms",
    summary: "Weak isolation levels let concurrent transactions interfere in subtle ways. Know the anomalies and the specific fixes for each.",
    points: ["Lost update: two read-modify-write cycles interleave, and one overwrites the other (counters, JSON document edits, wiki pages).", "Fixes for lost updates: atomic operations (UPDATE t SET n = n + 1), explicit locks (SELECT … FOR UPDATE), automatic detection (PostgreSQL repeatable read, Oracle serializable, SQL Server snapshot, but not MySQL InnoDB repeatable read), or compare-and-set (UPDATE … WHERE value = old).", "In replicated or multi-leader stores, locks and CAS don't work. Use commutative atomic operations or siblings plus merge.", "Write skew: two transactions read the same data, make a decision, then write different objects, so the combined result breaks an invariant. Examples: two doctors both going off call, double-booking a room, two users claiming the same username, double spending.", "Phantom: a write in one transaction changes the result of a search query in another (e.g. 'no bookings overlap this slot' becomes false).", "Fixes for write skew: serializable isolation; lock the rows the decision depends on (FOR UPDATE); unique or exclusion constraints; or as a last resort, materialise conflicts by creating lockable rows (e.g. one row per room-time slot)."],
    use: ["Any check-then-act logic: bookings, balances, inventory, uniqueness"], pitfalls: ["Assuming the default isolation level protects read-modify-write code", "ORMs that hide read-modify-write cycles"] });
  add({ id: "mvcc", cat: "Transactions", title: "Snapshot Isolation & MVCC",
    summary: "Each transaction reads from a consistent snapshot as of when it started. Readers never block writers, and writers never block readers.",
    points: ["It prevents read skew (non-repeatable reads), which matters for backups, long analytic queries and integrity checks that must see a consistent point in time.", "Multi-version concurrency control keeps several committed versions of each row. Each version is tagged with the ID of the transaction that created it and the one that deleted it.", "Visibility rule: ignore writes from transactions that were in progress or aborted when you started, and from transactions that started after you.", "Garbage collection (e.g. PostgreSQL VACUUM) removes versions no transaction can see any more.", "Indexes point to all versions and filter, or use append-only / copy-on-write B-trees where each transaction sees its own root (CouchDB, LMDB).", "Naming chaos: Oracle calls snapshot isolation 'serializable'; PostgreSQL and MySQL call it 'repeatable read'. The SQL standard's levels predate snapshot isolation."],
    use: ["The default in most modern databases for good reason"], pitfalls: ["Very long-running transactions preventing garbage collection (table bloat)"] });
  add({ id: "serializability", cat: "Transactions", title: "Serializability: Serial Execution, 2PL & SSI",
    summary: "The strongest isolation: the outcome is as if transactions ran one at a time. There are three ways to get it, with very different performance.",
    points: ["Actual serial execution: run every transaction on a single thread. It's feasible now that RAM is cheap and OLTP transactions are short. It requires stored procedures (no back-and-forth interactive transactions) and partitioning data per CPU core to scale. Cross-partition transactions are slow (VoltDB, Redis, Datomic).", "Two-phase locking (2PL): readers take shared locks, writers take exclusive locks, and all locks are held until commit. Readers block writers and writers block readers. Deadlocks are detected and a victim aborted. Predicate or index-range (next-key) locks prevent phantoms. Throughput and tail latency suffer.", "Serializable snapshot isolation (SSI): optimistic. Run on a snapshot, track reads that became stale (MVCC reads ignoring an uncommitted write, and writes that affect prior reads), and abort offenders at commit. It performs well when contention is low (PostgreSQL SERIALIZABLE, CockroachDB, FoundationDB).", "Pessimistic (2PL) vs optimistic (SSI): pessimism wins under heavy contention; optimism wins when conflicts are rare."],
    use: ["When correctness under concurrency matters more than peak throughput"], pitfalls: ["Long interactive transactions under 2PL", "High-contention hot rows under SSI (abort storms)"],
    table: [["", "Serial execution", "2PL", "SSI"], ["Style", "No concurrency", "Pessimistic locks", "Optimistic, detect at commit"], ["Readers block writers?", "n/a", "Yes", "No"], ["Needs", "Stored procedures, fits in RAM", "Deadlock detection", "Conflict tracking"], ["Weak spot", "Cross-partition txns", "Throughput & latency", "High contention → aborts"], ["Examples", "VoltDB, Redis", "MySQL/SQL Server serializable", "PostgreSQL serializable"]] });

  /* Chapter 8 */
  add({ id: "partial-failures", cat: "Faults & Time", title: "Partial Failures & Unreliable Networks",
    summary: "In a distributed system some parts fail while others keep working, nondeterministically. Over an asynchronous network you can't tell a slow node from a dead one.",
    points: ["A single computer is designed to be deterministic: it works or it crashes. Distributed systems must keep serving through partial failures (unlike HPC jobs, which just restart from a checkpoint).", "When a request gets no response, any of these could be true: the request was lost, it's queued, the node crashed, it paused, the response was lost, or the response is delayed. They're indistinguishable. The only tool is a timeout.", "Network faults are common in practice: misconfigurations, switch failures, one-way partitions, even physical cable damage. Software must be tested against them.", "Detecting faults: a TCP RST if the process died but the OS is alive, a script notifying peers, switch-level link status. Usually, though, you only get a timeout.", "Unbounded delays come from queueing: network switches, OS receive queues, VM scheduling, TCP flow control and retransmits. Pick timeouts experimentally or adaptively (TCP RTO, phi-accrual).", "Telephone circuits reserve bandwidth, so delay is bounded. Packet-switched networks share bandwidth, so delay is variable. That's a trade-off of utilisation for predictability, and it's why datacenter networks have no latency guarantees."],
    use: ["Setting timeouts, designing health checks and failure detectors"], pitfalls: ["Timeouts too short (false positives cause failover storms) or too long (slow recovery)"] });
  add({ id: "unreliable-clocks", cat: "Faults & Time", title: "Unreliable Clocks & Process Pauses",
    summary: "Clocks drift and jump, and a thread can freeze for seconds without knowing it. Never assume synchronised time or uninterrupted execution.",
    points: ["Time-of-day (wall) clocks can jump backwards or forwards when NTP syncs them. Monotonic clocks only move forward and are meant for measuring durations (timeouts); comparing them across machines is meaningless.", "Quartz drift can reach tens of milliseconds per minute of resync interval, and seconds per day. NTP is limited by network delay, and firewalled or misconfigured nodes drift silently. Leap seconds have crashed systems. VMs add more jitter.", "Danger: last-write-wins by timestamp across nodes can drop a write that actually happened later on a node with a slower clock.", "Treat clock readings as intervals with a confidence bound. Google's TrueTime exposes [earliest, latest], and Spanner waits out the uncertainty before committing to get globally ordered snapshots.", "Process pauses: stop-the-world GC, VM suspension and live migration, laptop sleep, context switches, synchronous disk I/O, page faults and swapping, SIGSTOP. Code that checks 'my lease is still valid' and then acts can be paused in between.", "Hard real-time systems bound response times but are expensive. Alternatives: treat GC like a planned outage (drain traffic first), or restart processes before the heap fills."],
    use: ["Lease-based locks, timestamps, ordering, timeouts"], pitfalls: ["Using System.currentTimeMillis() for ordering events across nodes", "Assuming no time passes between checking a lease and using it"] });
  add({ id: "fencing-tokens", cat: "Faults & Time", anim: "fencing", title: "Truth by Majority, Leases & Fencing Tokens",
    summary: "A node can't trust its own view of the world. The majority (quorum) decides, and fencing tokens stop a node that thinks it's still in charge.",
    points: ["A node can be declared dead by the others while it's merely slow or paused. Decisions such as 'who is leader' or 'who holds the lock' are made by a quorum, and a node must defer to it.", "Leases (locks with timeouts) expire, but the former holder may wake up after a pause still believing it holds the lease and write to shared storage, corrupting data.", "Fencing: the lock service issues a monotonically increasing token with each lease. The storage service remembers the highest token seen and rejects writes with older tokens.", "ZooKeeper's transaction ID (zxid) or node version can serve as the fencing token. The storage side must cooperate (check the tokens)."],
    use: ["Distributed locks protecting shared resources (files, leader-only jobs)"], pitfalls: ["Trusting Redis/any lock with expiry for correctness without fencing"] });
  add({ id: "byzantine-system-models", cat: "Faults & Time", title: "Byzantine Faults & System Models",
    summary: "Algorithms are proved correct under a system model: assumptions about timing and failures. Choose one close to reality.",
    points: ["Byzantine faults: nodes may lie or behave arbitrarily (bugs, corruption, malice). Byzantine fault-tolerant protocols need more than two-thirds honest nodes. They matter in aerospace and blockchains. Datacenter systems usually assume non-Byzantine nodes, but still add cheap protection: checksums, input validation, multiple NTP servers.", "Timing models: synchronous (bounded delays; unrealistic), partially synchronous (usually behaves, sometimes doesn't; realistic), asynchronous (no timing assumptions at all).", "Node failure models: crash-stop, crash-recovery (nodes restart with durable state intact; realistic), Byzantine.", "Safety properties ('nothing bad happens', e.g. uniqueness of fencing tokens) must always hold. Liveness properties ('something good eventually happens', e.g. availability) may have caveats.", "Models are simplifications: real disks lose data and hardware misbehaves, so production code still needs defensive checks for 'impossible' states."],
    use: ["Reasoning about what a consensus or replication algorithm actually guarantees"], pitfalls: ["Assuming the network or clocks are synchronous"] });

  /* Chapter 9 */
  add({ id: "linearizability", cat: "Distributed Systems", title: "Linearizability",
    summary: "The strongest single-object consistency: the system behaves as if there were one copy of the data, and once any read sees a new value, all later reads do too.",
    points: ["It's a recency guarantee on individual objects (registers). There must be a single point in time at which each operation takes effect.", "Not the same as serializability, which is an isolation property of multi-object transactions. Having both is 'strict serializability'. Serializable snapshot isolation is not linearizable.", "What needs it: locks and leader election (everyone must agree who holds it), uniqueness constraints (usernames, seats, balances), and cross-channel timing (a file stored, then a message sent over a queue telling a worker to process it).", "Implementations: single-leader can be linearizable if you read from the leader and it really is the leader; consensus-based systems are; multi-leader is not; leaderless with quorums is usually not (LWW, sloppy quorums, clock skew).", "Cost (CAP): if a partition cuts off replicas, a linearizable system must make some of them unavailable. Linearizability is also slow even without faults, which is why many databases (and even CPU caches in multi-core RAM) don't provide it."],
    use: ["Coordination: leader election, locks, uniqueness"], pitfalls: ["Assuming quorum reads and writes are automatically linearizable"],
    diagram: { nodes: [
      n("ref", "Referee", "writes result", "client", 80, 260, "Writes the final score."),
      n("lead", "Leader", "", "db", 300, 260, "Receives the write."),
      n("f1", "Follower 1", "up to date", "db", 560, 110, "Replicated quickly."),
      n("f2", "Follower 2", "lagging", "db", 560, 410, "Replication delayed."),
      n("alice", "Alice", "", "client", 840, 110, "Reads from Follower 1."),
      n("bob", "Bob", "", "client", 840, 410, "Reads from Follower 2.")
    ], edges: [["ref", "lead"], ["lead", "f1"], ["lead", "f2"], ["alice", "f1"], ["bob", "f2"], ["alice", "bob"]], flows: [{ name: "Non-linearizable read", steps: [
      ["ref", "lead", "Final score: 1–0"],
      ["lead", "f1", "Replicated to Follower 1 quickly"],
      ["alice", "f1", "Refresh → 'Final: 1–0!'"],
      ["alice", "bob", "Alice shouts 'The match is over!'"],
      ["bob", "f2", "Bob refreshes AFTER hearing Alice…"],
      ["f2", "bob", "…and sees 'still 0–0, in progress'. His later read returned an older value, so this is not linearizable"],
      ["lead", "f2", "Replication finally arrives. A linearizable system would behave as if there were a single copy"]
    ]}]} });
  add({ id: "ordering-causality", cat: "Distributed Systems", title: "Ordering, Causality & Lamport Timestamps",
    summary: "Causality gives a partial order (cause before effect). Linearizability gives a total order. Causal consistency is the strongest model that stays available during network partitions.",
    points: ["Causal dependencies are everywhere: a question before its answer, a row created before it's updated, a snapshot consistent with causality, a read that informed a write.", "Linearizability implies causality but costs availability and latency. Causal consistency gives most of the benefit without coordination on every operation (research systems such as COPS).", "Sequence numbers from a single leader's log give a total order consistent with causality. Without a single leader, per-node counters, physical timestamps or pre-allocated blocks are not causally consistent.", "Lamport timestamps: each node keeps (counter, nodeID). Every message carries the max counter seen; receivers jump to it and increment. The result is a total order consistent with causality, compact compared with version vectors.", "But Lamport order is only known after the fact. To decide 'is this username taken?' right now, you need to know no earlier claim is still in flight. That needs total order broadcast."],
    use: ["Designing replication and conflict detection, understanding why consensus is needed"], pitfalls: ["Using wall-clock timestamps from different nodes as an ordering"] });
  add({ id: "total-order-broadcast", cat: "Distributed Systems", title: "Total Order Broadcast (Atomic Broadcast)",
    summary: "Deliver every message to every node reliably, in the same order. This is the foundation of state machine replication, and it's equivalent to consensus.",
    points: ["Two properties: reliable delivery (if one node gets a message, all eventually do) and totally ordered delivery (all nodes see the same order). The order is fixed at delivery, never changed afterwards.", "State machine replication: if every replica applies the same deterministic writes in the same order, they stay consistent. This is how databases replicate and how serializable transactions run as stored procedures.", "It's like appending to a log. ZooKeeper and etcd implement it; Kafka gives it per partition.", "Build linearizable writes on it: append 'I claim username X', read the log back, and the first claim in log order wins. Linearizable reads need a sync as well: read after your own log position, or use quorum reads.", "Consensus and total order broadcast are equivalent problems: each can implement the other."],
    use: ["Replicated state machines, uniqueness constraints, fencing tokens, distributed logs"], pitfalls: ["Expecting a single totally ordered log to scale beyond one leader's throughput"],
    diagram: { nodes: [
      n("wa", "Writer A", "", "client", 80, 120, "Submits x := 1."),
      n("wb", "Writer B", "", "client", 80, 400, "Concurrently submits x := 2."),
      n("log", "Total-order log", "consensus-backed", "queue", 360, 260, "Assigns one global sequence to all messages."),
      n("r1", "Replica 1", "", "db", 660, 100, "Applies messages in log order."),
      n("r2", "Replica 2", "", "db", 660, 260, "Applies messages in log order."),
      n("r3", "Replica 3", "", "db", 660, 420, "Applies messages in log order.")
    ], edges: [["wa", "log"], ["wb", "log"], ["log", "r1"], ["log", "r2"], ["log", "r3"]], flows: [{ name: "State machine replication", steps: [
      ["wa", "log", "x := 1"], ["wb", "log", "x := 2 (concurrent)"],
      ["log", "r1", "Deliver #41 x:=1, then #42 x:=2"], ["log", "r2", "Exactly the same order"], ["log", "r3", "Same order, so every replica ends with x = 2"]
    ]}]} });
  add({ id: "coordination-services", cat: "Distributed Systems", title: "Coordination Services (ZooKeeper, etcd)",
    summary: "Small, strongly consistent, fault-tolerant key-value stores that do the hard consensus work so your system doesn't have to.",
    points: ["They hold small amounts of data (config, membership, locks) replicated on 3–5 nodes via consensus (ZAB in ZooKeeper, Raft in etcd).", "Features: linearizable atomic operations (compare-and-set for locks), a total order of operations (zxid, usable as a fencing token), failure detection through sessions and ephemeral nodes that disappear when a client dies, and change notifications (watches).", "Uses: leader election, assigning partitions or work to nodes and rebalancing on membership changes, service discovery, and cluster membership.", "Not for high-rate application data: the data should change slowly (minutes or hours), not thousands of times per second.", "Trend: systems embedding their own Raft instead (Kafka KRaft replacing ZooKeeper)."],
    use: ["HBase, Hadoop YARN, Kafka (legacy), Kubernetes (etcd), Consul"], pitfalls: ["Storing high-churn data in ZooKeeper", "Running an even number of nodes"] });

  /* Chapter 10 */
  add({ id: "unix-philosophy", cat: "Batch Processing", title: "Batch Processing & the Unix Philosophy",
    summary: "Services respond to requests, batch jobs crunch bounded input into output, and stream processors run in between. Batch processing inherits Unix's composability.",
    points: ["Three kinds of systems: online services (measured by response time), batch jobs (bounded input, measured by throughput, run periodically), and stream processors (near-real-time, unbounded input).", "A Unix pipeline such as cat log | awk '{print $7}' | sort | uniq -c | sort -rn | head analyses gigabytes of logs quickly. GNU sort spills to disk and uses all cores, so it handles more data than fits in memory.", "Unix principles: each program does one thing well; the output of one becomes the input of another; there's a uniform interface (a file or stream of bytes, lines as records); and logic is separate from wiring (stdin/stdout).", "Immutable inputs make experimentation safe: rerun as often as you like, inspect intermediate output, restart part of a pipeline.", "MapReduce is essentially Unix-style tools distributed across thousands of machines, with a distributed filesystem playing the role of files."],
    use: ["Log analysis, ad-hoc data exploration before building infra"], pitfalls: ["Building a cluster job for data a single machine can process in minutes"] });
  add({ id: "mapreduce", cat: "Batch Processing", anim: "mapreduce", title: "MapReduce & Distributed Filesystems",
    summary: "Split input across machines, run a mapper per record to emit key-value pairs, shuffle by key, and reduce each key's values. Fault tolerance comes from immutable inputs and re-runnable tasks.",
    points: ["HDFS: a shared-nothing distributed filesystem. A NameNode tracks where file blocks live; blocks are replicated (or erasure-coded) across DataNodes. Object stores such as S3 play the same role today.", "Execution: read input splits → mapper extracts (key, value) per record → the framework partitions by key hash, sorts, and copies partitions to reducers (the shuffle) → the reducer iterates over all values for each key → output files.", "It brings computation to the data: the scheduler runs mappers on machines that hold a replica of the input (data locality).", "Workflows chain jobs, each writing output files that the next reads. Schedulers such as Airflow or Oozie manage the dependencies.", "Fault tolerance: if a task fails, rerun it. Inputs are immutable and partial outputs are discarded, which suits clusters where low-priority tasks get preempted.", "Combiners pre-aggregate map output locally to shrink the shuffle."],
    use: ["Large offline ETL, building indexes, log processing (Hadoop legacy; Spark today)"], pitfalls: ["Writing every intermediate step to HDFS (slow; see dataflow engines)"] });
  add({ id: "batch-joins", cat: "Batch Processing", title: "Joins & Outputs in Batch Processing",
    summary: "Batch jobs join entire datasets at once. Choose the join algorithm by data size and layout, and write immutable outputs that are easy to roll back.",
    points: ["Reduce-side sort-merge join: mappers emit the join key from both inputs (e.g. activity events and user profiles). A secondary sort delivers the user record first, and the reducer streams the matching events. It works on any data but is expensive (a full shuffle).", "Skew / hot keys (e.g. a celebrity): handle them with skewed joins that spread a hot key across reducers and replicate the other side, or a two-stage aggregation.", "Map-side joins (no shuffle): broadcast hash join (the small input fits in memory on every mapper), partitioned hash join (both inputs partitioned the same way), map-side merge join (both inputs also sorted by key).", "Output of batch workflows: build search indexes (Google's original use), train ML models, or build key-value database files inside the job and bulk-load them into read-only serving stores. Never write row by row into a live database from a batch job.", "Immutable outputs give 'human fault tolerance': deploy buggy code, then roll back to the previous output.", "Hadoop vs MPP databases: Hadoop allows arbitrary file formats (dump raw data first, interpret it later, as in a data lake), arbitrary processing code beyond SQL, and is designed for frequent task failure."],
    use: ["Enriching event logs with dimension data, building derived datasets"], pitfalls: ["Writing to a production DB from thousands of mappers (overload, partial results)"] });
  add({ id: "dataflow-engines", cat: "Batch Processing", title: "Beyond MapReduce: Dataflow Engines & Graph Processing",
    summary: "Spark, Flink and Tez treat a whole workflow as one DAG of operators. That's faster and more flexible than chaining MapReduce jobs through HDFS.",
    points: ["Materialising every intermediate result to replicated HDFS is wasteful. Jobs can't start until the previous job fully finishes, mappers are often redundant, and temporary data gets triple-replicated.", "Dataflow engines pipeline operators, sort only when needed, keep intermediate data in memory or on local disk, and schedule with locality awareness.", "Fault tolerance without materialisation: recompute lost partitions from their lineage (Spark RDDs) or checkpoints (Flink). Operators must be deterministic for this to be safe.", "Iterative graph algorithms (PageRank) fit the Pregel / bulk synchronous parallel model: in each superstep, every vertex processes messages sent along its edges and sends new ones (Giraph, Spark GraphX).", "High-level APIs (Hive, Spark SQL, DataFrames) are declarative, so the engine chooses join algorithms and uses vectorised, columnar execution."],
    use: ["Modern batch ETL and ML pipelines, graph analytics"], pitfalls: ["Non-deterministic operators (random, hash map iteration order) breaking recomputation"] });

  /* Chapter 11 */
  add({ id: "stream-time-windows", cat: "Messaging & Streaming", anim: "windows", title: "Reasoning About Time: Event Time & Windows",
    summary: "Stream processors aggregate over windows of time. Use the time the event happened, not the time it arrived, and decide what to do with stragglers.",
    points: ["Event time vs processing time: backlogs, restarts and mobile devices going offline make events arrive late or out of order. Windowing by processing time produces fake spikes.", "You never know a window is complete. A watermark declares 'probably no more events before T'. For stragglers: ignore and count them, or publish corrections.", "Whose clock? Device clocks lie. Log three timestamps: the device's event time, the device's send time and the server's receive time. The difference between the last two estimates the device clock offset.", "Window types: tumbling (fixed, non-overlapping), hopping (fixed, overlapping by a hop), sliding (all events within an interval of each other), and session (closed after a gap of inactivity).", "Uses of stream processing: complex event processing (standing queries matching patterns), stream analytics (rates, rolling averages, probabilistic sketches), maintaining materialised views and caches, and search on streams (stored queries matched against new documents)."],
    use: ["Real-time metrics, fraud detection, sessionisation, alerting"], pitfalls: ["Processing-time windows for business metrics", "Waiting forever for completeness"] });
  add({ id: "stream-joins", cat: "Messaging & Streaming", title: "Stream Joins",
    summary: "Streams join with other streams, with tables, or two table changelogs with each other. Each kind needs the processor to keep state.",
    points: ["Stream-stream (window) join: e.g. match search events with click events within an hour to compute click-through rate. The processor indexes recent events from both sides by key within the window.", "Stream-table join (enrichment): join each activity event with the user's profile. Instead of querying a remote DB per event, keep a local copy of the table, updated via its CDC stream.", "Table-table join (materialised view maintenance): both inputs are changelogs, and the output is a continuously updated join. Example: a home timeline cache is the join of the 'posts' and 'follows' changelogs.", "Time dependence: if the table changes, which version applies to an event? Joins become nondeterministic when replayed. Use versioned dimension records, a 'slowly changing dimension' (e.g. the tax rate at time of sale)."],
    use: ["Enrichment pipelines, real-time CTR, incremental view maintenance"], pitfalls: ["Per-event remote DB lookups (slow, overloads the DB)"],
    diagram: { nodes: [
      n("app", "Web app", "activity events", "client", 80, 110, "Emits page view and click events."),
      n("ev", "Activity stream", "Kafka", "queue", 300, 110, "Events keyed by user_id."),
      n("udb", "User DB", "profiles", "db", 80, 410, "System of record for profiles."),
      n("cdc", "User changelog", "CDC", "queue", 300, 410, "Every profile change as an event."),
      n("sp", "Stream processor", "local profile copy", "worker", 560, 260, "Holds user profiles in local state and joins each event with the current profile."),
      n("out", "Enriched events", "Kafka", "queue", 820, 260, "Events plus user attributes, ready for analytics.")
    ], edges: [["app", "ev"], ["udb", "cdc"], ["ev", "sp"], ["cdc", "sp"], ["sp", "out"]], flows: [{ name: "Stream-table join", steps: [
      ["udb", "cdc", "User 7 updates country = DE (CDC)"],
      ["cdc", "sp", "The processor updates its local profile table"],
      ["app", "ev", "User 7 views a page"],
      ["ev", "sp", "Look up user 7 locally (no remote call)"],
      ["sp", "out", "Emit {page view, user 7, country DE, …}"]
    ]}]} });
  add({ id: "stream-fault-tolerance", cat: "Messaging & Streaming", title: "Fault Tolerance in Stream Processing",
    summary: "A stream never ends, so you can't just rerun the whole job. Get 'effectively-once' results with checkpoints, atomic commits or idempotence.",
    points: ["Micro-batching: chop the stream into ~1-second batches, each treated like a small batch job (Spark Streaming).", "Checkpointing: periodically snapshot operator state, triggered by barriers flowing through the stream. On failure, restart from the last checkpoint (Flink).", "Neither prevents duplicate external side effects on their own. You need either atomic commit of outputs, state changes and input offsets together (Kafka transactions, Google Dataflow), or idempotent writes.", "Idempotence: store the input offset with each written value so replays are detectable. Fence off 'zombie' instances whose work was reassigned.", "Rebuilding state: keep state local for speed, and make it recoverable. Replicate it, log changes to a compacted topic (Kafka Streams changelogs), or rebuild from the input stream.", "'Exactly-once' in practice means the effects appear exactly once, not that the work ran exactly once."],
    use: ["Any stateful stream job with external outputs"], pitfalls: ["Non-idempotent sinks behind an at-least-once processor"] });

  /* Chapter 12 */
  add({ id: "derived-data", cat: "Derived Data & Correctness", title: "Systems of Record & Derived Data",
    summary: "Keep one authoritative source of truth, and derive everything else (caches, indexes, views, models) from it through an ordered log of changes.",
    points: ["System of record: the authoritative version, where each fact is written first. Derived data (caches, search indexes, materialised views, recommendations, ML models) can always be regenerated from it.", "No single tool does everything, so real applications combine specialised stores. The hard part is keeping them consistent.", "Avoid dual writes (the app writes to the DB and then the index; races and partial failures cause divergence). Instead, write once and propagate via CDC or event sourcing in one total order.", "Total ordering has limits: one leader's throughput, cross-datacenter latency, and microservices with separate storage. Causal ordering is often enough.", "Reprocessing: batch and stream derivations can be rebuilt from scratch. That enables schema migrations without big-bang cutovers: build the new view alongside the old and shift users over gradually.", "Lambda architecture runs batch and stream side by side. Kappa unifies them by replaying a log through the same streaming code."],
    use: ["Search, caching, analytics and ML all fed from one source"], pitfalls: ["Two sources of truth", "Dual writes from application code"],
    diagram: { nodes: [
      n("app", "Application", "writes", "service", 80, 260, "Writes each fact once, to the system of record."),
      n("sor", "System of record", "OLTP DB", "db", 290, 260, "The authoritative source of truth."),
      n("log", "Change log", "CDC → Kafka", "queue", 500, 260, "A totally ordered stream of changes."),
      n("idx", "Search index", "derived", "search", 780, 70, "Rebuildable from the log."),
      n("cache", "Cache", "derived", "cache", 780, 200, "Rebuildable from the log."),
      n("wh", "Warehouse", "derived", "analytics", 780, 330, "Rebuildable from the log."),
      n("ml", "ML features", "derived", "worker", 780, 460, "Rebuildable from the log.")
    ], edges: [["app", "sor"], ["sor", "log"], ["log", "idx"], ["log", "cache"], ["log", "wh"], ["log", "ml"]], flows: [{ name: "Derive, don't dual-write", steps: [
      ["app", "sor", "Write the fact once"], ["sor", "log", "CDC captures it, in commit order"],
      ["log", "idx", "The indexer consumes it"], ["log", "cache", "The cache updater consumes it"], ["log", "wh", "The loader consumes it"], ["log", "ml", "The feature pipeline consumes it. Any view can be rebuilt by replaying the log"]
    ]}]} });
  add({ id: "unbundling", cat: "Derived Data & Correctness", title: "Unbundling the Database & Dataflow Applications",
    summary: "Secondary indexes, materialised views and replication are all derived data inside one database. Unbundle them into separate systems connected by logs, and design applications as dataflow.",
    points: ["A database already maintains derived structures internally (indexes, materialised views, replicas). Unbundling does the same across different systems, connected by event logs.", "Two directions: federated reads (one query interface over many stores, i.e. a polystore) and unbundled writes (sync many stores through ordered logs with idempotent consumers).", "Loose coupling via logs: a slow or failed consumer doesn't break others, and teams can own their derived systems independently.", "Designing apps around dataflow: application code becomes derivation functions that transform input streams into outputs, much like a spreadsheet recalculating cells. Keep state in logs and derived stores rather than hidden in stateless services that call each other.", "Write path vs read path: work done eagerly when data is written (precomputed indexes and caches) vs lazily when read. Derived data moves the boundary between them.", "Push state changes to clients (subscriptions, offline-first apps, end-to-end event streams) instead of polling. Even reads can be modelled as events."],
    use: ["Architecting multi-store systems, real-time UIs"], pitfalls: ["Synchronous RPC chains where a stream would decouple"] });
  add({ id: "end-to-end-correctness", cat: "Derived Data & Correctness", title: "Aiming for Correctness: End-to-End Integrity",
    summary: "Exactly-once in each component doesn't guarantee correctness end to end. Enforce idempotence and constraints at the edges, and prioritise integrity over timeliness.",
    points: ["End-to-end argument: only the application at the endpoints can fully guarantee properties like deduplication. TCP dedup and DB transactions each cover one hop, but a user double-clicking 'pay' creates two separate requests. Generate a unique request ID at the client and store it with a uniqueness constraint.", "Enforcing uniqueness in log-based systems: partition requests by the unique value (e.g. username) and have a single-threaded processor per partition decide in log order. This is essentially consensus via the log.", "Multi-partition operations without atomic commit: log the request once with an ID, then deterministically derive the debit and credit events from it.", "Timeliness (users see up-to-date state) vs integrity (no data loss, no contradictions). A timeliness violation is temporary (eventual consistency); an integrity violation is permanent. Most apps can relax timeliness but never integrity.", "Loosely enforced constraints: many business constraints can be fixed after the fact (overbooking → apologise and compensate), which allows coordination-avoiding designs.", "Trust, but verify: databases and hardware have bugs. Audit continuously (checksums, end-to-end verification, reconciliation), and prefer event-sourced, deterministic designs that can be re-derived and checked. Cryptographic tools like Merkle trees help."],
    use: ["Payments, bookings, any system where duplicates or loss are unacceptable"], pitfalls: ["Relying on 'exactly-once' marketing claims without end-to-end IDs"],
    diagram: { nodes: [
      n("u1", "User 1", "claim 'alice'", "client", 80, 120, "Request ID r1."),
      n("u2", "User 2", "claim 'alice'", "client", 80, 400, "Request ID r2."),
      n("log", "Requests log", "partitioned by username", "queue", 340, 260, "All claims for 'alice' land in one partition, in order."),
      n("proc", "Processor", "single-threaded / partition", "worker", 600, 260, "Decides deterministically in log order."),
      n("names", "Usernames", "derived state", "db", 860, 260, "Taken names.")
    ], edges: [["u1", "log"], ["u2", "log"], ["log", "proc"], ["proc", "names"]], flows: [{ name: "Uniqueness via a log", steps: [
      ["u1", "log", "r1: claim 'alice'"], ["u2", "log", "r2: claim 'alice' (concurrently)"],
      ["log", "proc", "Delivered in log order: r1, then r2"],
      ["proc", "names", "r1 wins → 'alice' taken"],
      ["proc", "u1", "Success (a retry of r1 is deduped by request ID)"],
      ["proc", "u2", "Rejected: already taken"]
    ]}]} });
  add({ id: "data-ethics", cat: "Derived Data & Correctness", title: "Doing the Right Thing: Ethics & Privacy",
    summary: "Data systems affect people's lives. Engineers share responsibility for how data is collected, used and protected.",
    points: ["Predictive analytics (credit, insurance, hiring, policing) can lock people out of opportunities based on patterns in historical data, reproducing existing bias and offering little recourse or explanation.", "Accountability: automated decisions need transparency, a way to appeal, and humans responsible for outcomes. Watch for feedback loops that amplify errors.", "Tracking and surveillance: behavioural data collection often serves the service provider more than the user. Consent through terms nobody reads isn't meaningful choice when opting out means exclusion.", "Privacy means the user decides what to reveal to whom. Collected data is a liability as well as an asset: it can leak, be subpoenaed, or be misused by future owners.", "Practices: collect only what's needed, delete when no longer needed (data minimisation, purpose limitation, as in GDPR), encrypt, allow users to see and delete their data, and consider who could misuse it."],
    use: ["Design reviews for any system handling personal data"], pitfalls: ["'Collect everything, it might be useful later'"] });

  /* ---------------------------------- glossary & quiz ---------------------------------- */
  SD.glossary.push(
    ["Avro", "Binary encoding that resolves the writer's schema against the reader's schema by field name."],
    ["Backward compatibility", "Newer code can read data written by older code."],
    ["Forward compatibility", "Older code can read data written by newer code (ignoring unknown fields)."],
    ["Protocol Buffers / Thrift", "Schema-based binary encodings that identify fields by numeric tags."],
    ["Schema-on-read", "Structure is implicit and interpreted when data is read (document stores)."],
    ["Star schema", "Warehouse layout: a central fact table referencing dimension tables."],
    ["Column-oriented storage", "Storing each column's values together for fast scans and compression."],
    ["Materialized view", "A stored, precomputed query result kept up to date as data changes."],
    ["Data cube", "A grid of precomputed aggregates along several dimensions."],
    ["Read-your-writes", "Consistency guarantee that users always see their own updates."],
    ["Monotonic reads", "Guarantee that a user never sees older data after having seen newer data."],
    ["Consistent prefix reads", "Readers see causally related writes in the order they happened."],
    ["Sloppy quorum", "Accepting writes on nodes outside a key's home replicas during failures."],
    ["Version vector", "A per-replica counter set used to detect concurrent writes."],
    ["Happens-before", "B depends on or knew about A. Otherwise they are concurrent."],
    ["Dirty read", "Reading another transaction's uncommitted write."],
    ["Read skew", "Seeing different parts of the database at different points in time (non-repeatable read)."],
    ["Lost update", "Concurrent read-modify-write cycles where one overwrites the other."],
    ["Write skew", "Two transactions read the same data, then write different rows, violating an invariant."],
    ["Phantom", "A write that changes the result of another transaction's search query."],
    ["Snapshot isolation", "Each transaction reads from a consistent snapshot as of its start."],
    ["MVCC", "Multi-version concurrency control: keeping several versions of rows for snapshots."],
    ["Two-phase locking (2PL)", "Shared and exclusive locks held until commit, to achieve serializability."],
    ["Serializable snapshot isolation (SSI)", "Optimistic serializability that detects stale reads at commit time."],
    ["Monotonic clock", "A clock that only moves forward. Use it for measuring durations, not for timestamps."],
    ["Byzantine fault", "A node behaving arbitrarily or maliciously, e.g. lying."],
    ["Safety / liveness", "'Nothing bad happens' vs 'something good eventually happens'."],
    ["Lamport timestamp", "A (counter, node ID) pair giving a total order consistent with causality."],
    ["Total order broadcast", "Reliable delivery of all messages to all nodes in the same order."],
    ["Causal consistency", "Operations that are causally related are seen in the same order by everyone."],
    ["Shuffle", "The MapReduce phase that partitions, sorts and moves mapper output to reducers."],
    ["Sort-merge join", "A reduce-side join where records from both inputs meet at the same reducer by key."],
    ["Broadcast hash join", "A map-side join that loads the small input into memory on every mapper."],
    ["Straggler event", "An event that arrives after its window was considered complete."],
    ["Tumbling / hopping / session window", "Fixed non-overlapping, fixed overlapping, and gap-based time windows."],
    ["System of record", "The authoritative source of truth from which other data is derived."],
    ["Derived data", "Data produced from other data (caches, indexes, views) that can be rebuilt."],
    ["End-to-end argument", "Guarantees like deduplication must ultimately be implemented at the endpoints."]
  );
  SD.quiz.push(
    { q: "A user posts a comment, reloads, and it's gone. Then it reappears. Which guarantee was violated?", o: ["Monotonic reads", "Read-your-writes", "Consistent prefix", "Linearizability of locks"], a: 1, why: "They couldn't read their own write: the reload hit a lagging replica. Route reads of user-modified data to the leader." },
    { q: "Two doctors both go off call because each saw 'two on call'. What is this, and what prevents it?", o: ["Dirty read, read committed", "Lost update, atomic increment", "Write skew, serializable isolation", "Phantom read, read uncommitted"], a: 2, why: "Each transaction wrote a different row based on a stale premise. Only serializable isolation (or locking the rows the check depends on) prevents it." },
    { q: "Snapshot isolation is usually implemented with…", o: ["Two-phase commit", "MVCC", "Vector clocks", "Leaderless quorums"], a: 1, why: "Multi-version concurrency control keeps old committed versions so each transaction sees a consistent snapshot." },
    { q: "A lock holder pauses for 20 s in GC; its lease expires and another client takes over. What protects storage?", o: ["Longer leases", "NTP", "Fencing tokens", "Read replicas"], a: 2, why: "Storage rejects writes carrying a token lower than the highest it has seen, so the zombie holder can't corrupt data." },
    { q: "Which encoding identifies fields by numeric tags in the data?", o: ["Avro", "Protocol Buffers", "CSV", "Java Serializable"], a: 1, why: "Protobuf and Thrift encode tag numbers. Avro relies on writer and reader schema resolution by field name." },
    { q: "Adding a REQUIRED field in schema v2 breaks…", o: ["Forward compatibility", "Backward compatibility", "Neither", "Only JSON"], a: 1, why: "New code (v2) reading old data can't find the required field. Add new fields as optional or with defaults." },
    { q: "A term-partitioned (global) secondary index makes…", o: ["Reads scatter/gather", "Reads hit one partition; writes may touch several", "Writes always synchronous", "No difference"], a: 1, why: "The index is partitioned by the indexed value, so lookups are targeted but a document write can update several index partitions (usually asynchronously)." },
    { q: "To measure a request timeout on one machine, use…", o: ["Time-of-day clock", "Monotonic clock", "Lamport clock", "The database's NOW()"], a: 1, why: "Monotonic clocks never jump backwards, so they're safe for durations. Wall clocks can jump when NTP adjusts them." },
    { q: "Enriching click events with user profiles in a stream job is best done with…", o: ["A remote DB query per event", "A stream-table join using a local copy updated via CDC", "A nightly batch", "Two-phase commit"], a: 1, why: "Keep the table locally in the processor's state, updated from its changelog. No per-event network round trips." },
    { q: "Linearizability differs from serializability because it…", o: ["Is about multi-object transactions", "Is a recency guarantee on single objects", "Allows stale reads", "Requires 2PL"], a: 1, why: "Serializability is transaction isolation; linearizability says reads reflect the latest write, as if there were one copy." },
    { q: "A MapReduce join where the small table fits in memory on every mapper is a…", o: ["Sort-merge join", "Broadcast hash join", "Partitioned merge join", "Nested loop over HDFS"], a: 1, why: "Each mapper loads the small input into a hash table and streams the large input past it, with no shuffle needed." },
    { q: "Which is the strongest consistency model that doesn't give up availability during network partitions?", o: ["Linearizability", "Causal consistency", "Strict serializability", "Sequential consistency"], a: 1, why: "Causal consistency can be implemented without coordinating every operation, so it stays available under partitions." },
    { q: "Why avoid 'hash mod N' for assigning partitions to nodes?", o: ["It's slow to compute", "Changing N moves most keys", "It causes phantoms", "It needs ZooKeeper"], a: 1, why: "Use a fixed number of partitions, dynamic splitting, or consistent hashing so rebalancing moves minimal data." }
  );

  /* ---------------------------------- DDIA map ---------------------------------- */
  SD.ddia = {
    title: "Designing Data-Intensive Applications", author: "Martin Kleppmann",
    parts: ["Part I · Foundations of Data Systems", "Part II · Distributed Data", "Part III · Derived Data"],
    chapters: [
      { n: 1, part: 0, title: "Reliable, Scalable, and Maintainable Applications", summary: "What 'good' means for a data system: it keeps working despite faults, handles growth in load, and stays easy to operate and change.",
        takeaways: ["Tolerate faults so they don't become failures", "Describe load with parameters and performance with percentiles", "Most cost is maintenance: invest in operability, simplicity and evolvability"],
        sections: [["Thinking About Data Systems", ["reliability", "caching", "queues-pubsub"], 0], ["Reliability", ["reliability"], 0], ["Hardware Faults", ["reliability", "ha-dr"], 1], ["Software Errors", ["reliability", "circuit-breaker"], 1], ["Human Errors", ["reliability", "observability"], 1], ["How Important Is Reliability?", ["reliability", "availability"], 1], ["Scalability", ["scaling"], 0], ["Describing Load", ["load-parameters", "s:news-feed"], 1], ["Describing Performance", ["load-parameters", "latency-throughput"], 1], ["Approaches for Coping with Load", ["scaling", "sharding"], 1], ["Maintainability", ["maintainability"], 0], ["Operability", ["maintainability", "observability"], 1], ["Simplicity", ["maintainability"], 1], ["Evolvability", ["maintainability", "encoding-formats"], 1]] },
      { n: 2, part: 0, title: "Data Models and Query Languages", summary: "Relational, document and graph models suit different relationship shapes. Declarative query languages let engines optimise for you.",
        takeaways: ["Documents for trees and aggregates, relational for many-to-many, graphs for highly connected data", "Schema-on-read vs schema-on-write is like dynamic vs static typing", "Declarative beats imperative for data access"],
        sections: [["Relational Model Versus Document Model", ["data-models"], 0], ["The Birth of NoSQL", ["data-models"], 1], ["The Object-Relational Mismatch", ["data-models"], 1], ["Many-to-One and Many-to-Many Relationships", ["data-models", "normalization"], 1], ["Are Document Databases Repeating History?", ["data-models"], 1], ["Relational Versus Document Databases Today", ["data-models", "normalization"], 1], ["Query Languages for Data", ["query-languages"], 0], ["Declarative Queries on the Web", ["query-languages"], 1], ["MapReduce Querying", ["query-languages", "mapreduce"], 1], ["Graph-Like Data Models", ["graph-models"], 0], ["Property Graphs", ["graph-models"], 1], ["The Cypher Query Language", ["graph-models"], 1], ["Graph Queries in SQL", ["graph-models"], 1], ["Triple-Stores and SPARQL", ["graph-models"], 1], ["The Foundation: Datalog", ["graph-models"], 1]] },
      { n: 3, part: 0, title: "Storage and Retrieval", summary: "How databases lay out bytes: log-structured (LSM) vs update-in-place (B-tree) engines for OLTP, and column stores for analytics.",
        takeaways: ["LSM-trees favour writes; B-trees favour predictable reads", "OLTP and OLAP need different systems", "Column storage + compression + sort order make scans fast"],
        sections: [["Data Structures That Power Your Database", ["storage-engines"], 0], ["Hash Indexes", ["storage-engines"], 1], ["SSTables and LSM-Trees", ["storage-engines", "bloom-filter"], 1], ["B-Trees", ["storage-engines", "indexing"], 1], ["Comparing B-Trees and LSM-Trees", ["storage-engines"], 1], ["Other Indexing Structures", ["indexing", "geospatial"], 1], ["Transaction Processing or Analytics?", ["oltp-olap"], 0], ["Data Warehousing", ["oltp-olap"], 1], ["Stars and Snowflakes: Schemas for Analytics", ["oltp-olap"], 1], ["Column-Oriented Storage", ["column-storage"], 0], ["Column Compression", ["column-storage"], 1], ["Sort Order in Column Storage", ["column-storage"], 1], ["Writing to Column-Oriented Storage", ["column-storage"], 1], ["Aggregation: Data Cubes and Materialized Views", ["column-storage"], 1]] },
      { n: 4, part: 0, title: "Encoding and Evolution", summary: "Data outlives code. Schema-based binary encodings allow old and new versions to interoperate, whether data flows through databases, services or messages.",
        takeaways: ["Rolling upgrades need backward and forward compatibility", "Protobuf/Thrift use tags; Avro resolves writer vs reader schemas", "RPC is not a local call: plan for timeouts and retries"],
        sections: [["Formats for Encoding Data", ["encoding-formats"], 0], ["Language-Specific Formats", ["encoding-formats"], 1], ["JSON, XML, and Binary Variants", ["encoding-formats"], 1], ["Thrift and Protocol Buffers", ["encoding-formats"], 1], ["Avro", ["encoding-formats"], 1], ["The Merits of Schemas", ["encoding-formats"], 1], ["Modes of Dataflow", ["dataflow-modes"], 0], ["Dataflow Through Databases", ["dataflow-modes"], 1], ["Dataflow Through Services: REST and RPC", ["dataflow-modes", "api-styles"], 1], ["Message-Passing Dataflow", ["dataflow-modes", "queues-pubsub"], 1]] },
      { n: 5, part: 1, title: "Replication", summary: "Keeping copies on several machines: single-leader, multi-leader and leaderless designs, the anomalies replication lag causes, and how conflicts are detected.",
        takeaways: ["Async replication means eventual consistency; design for lag", "Multi-leader and leaderless setups need conflict resolution", "Concurrency is about knowledge, not clock time"],
        sections: [["Leaders and Followers", ["replication"], 0], ["Synchronous Versus Asynchronous Replication", ["replication"], 1], ["Setting Up New Followers", ["replication-logs"], 1], ["Handling Node Outages", ["replication-logs", "health-checks"], 1], ["Implementation of Replication Logs", ["replication-logs", "cdc-outbox"], 1], ["Problems with Replication Lag", ["replication-lag"], 0], ["Reading Your Own Writes", ["replication-lag", "consistency-models"], 1], ["Monotonic Reads", ["replication-lag"], 1], ["Consistent Prefix Reads", ["replication-lag"], 1], ["Solutions for Replication Lag", ["replication-lag"], 1], ["Multi-Leader Replication", ["multi-leader"], 0], ["Use Cases for Multi-Leader Replication", ["multi-leader", "multi-region", "s:collab-editor"], 1], ["Handling Write Conflicts", ["multi-leader"], 1], ["Multi-Leader Replication Topologies", ["multi-leader"], 1], ["Leaderless Replication", ["quorum", "s:kv-store"], 0], ["Writing to the Database When a Node Is Down", ["quorum", "s:kv-store"], 1], ["Limitations of Quorum Consistency", ["quorum"], 1], ["Sloppy Quorums and Hinted Handoff", ["quorum", "s:kv-store"], 1], ["Detecting Concurrent Writes", ["concurrent-writes", "clocks"], 1]] },
      { n: 6, part: 1, title: "Partitioning", summary: "Splitting data across nodes by key range or hash, handling secondary indexes, rebalancing as the cluster changes, and routing requests to the right node.",
        takeaways: ["Range partitioning enables scans; hash partitioning spreads load", "Local indexes scatter reads; global indexes scatter writes", "Never rebalance with hash mod N"],
        sections: [["Partitioning and Replication", ["sharding", "replication"], 0], ["Partitioning of Key-Value Data", ["sharding"], 0], ["Partitioning by Key Range", ["sharding"], 1], ["Partitioning by Hash of Key", ["sharding", "consistent-hashing"], 1], ["Skewed Workloads and Relieving Hot Spots", ["sharding", "cache-problems"], 1], ["Partitioning and Secondary Indexes", ["secondary-index-partitioning"], 0], ["Partitioning Secondary Indexes by Document", ["secondary-index-partitioning"], 1], ["Partitioning Secondary Indexes by Term", ["secondary-index-partitioning"], 1], ["Rebalancing Partitions", ["rebalancing"], 0], ["Strategies for Rebalancing", ["rebalancing", "consistent-hashing"], 1], ["Operations: Automatic or Manual Rebalancing", ["rebalancing"], 1], ["Request Routing", ["request-routing", "coordination-services", "gossip"], 0], ["Parallel Query Execution", ["request-routing"], 1]] },
      { n: 7, part: 1, title: "Transactions", summary: "What ACID really promises, the race conditions weak isolation allows, and the three ways to achieve serializability.",
        takeaways: ["Know which anomalies your isolation level permits", "Snapshot isolation stops read skew but not write skew", "Serial execution, 2PL and SSI make different trade-offs"],
        sections: [["The Slippery Concept of a Transaction", ["acid-base"], 0], ["The Meaning of ACID", ["acid-base"], 1], ["Single-Object and Multi-Object Operations", ["acid-base"], 1], ["Weak Isolation Levels", ["isolation-levels"], 0], ["Read Committed", ["isolation-levels"], 1], ["Snapshot Isolation and Repeatable Read", ["mvcc", "isolation-levels"], 1], ["Preventing Lost Updates", ["lost-updates-write-skew"], 1], ["Write Skew and Phantoms", ["lost-updates-write-skew", "s:ticket-booking"], 1], ["Serializability", ["serializability"], 0], ["Actual Serial Execution", ["serializability"], 1], ["Two-Phase Locking (2PL)", ["serializability"], 1], ["Serializable Snapshot Isolation (SSI)", ["serializability"], 1]] },
      { n: 8, part: 1, title: "The Trouble with Distributed Systems", summary: "Networks lose and delay messages, clocks lie, and processes pause. A node can't know the truth on its own, so correctness is defined by quorums and system models.",
        takeaways: ["Timeouts are the only failure detector, and they're imperfect", "Never rely on synchronised clocks for ordering", "Use quorums and fencing tokens, not a node's own opinion"],
        sections: [["Faults and Partial Failures", ["partial-failures"], 0], ["Cloud Computing and Supercomputing", ["partial-failures"], 1], ["Unreliable Networks", ["partial-failures"], 0], ["Network Faults in Practice", ["partial-failures"], 1], ["Detecting Faults", ["partial-failures", "health-checks", "gossip"], 1], ["Timeouts and Unbounded Delays", ["partial-failures", "retries"], 1], ["Synchronous Versus Asynchronous Networks", ["partial-failures"], 1], ["Unreliable Clocks", ["unreliable-clocks"], 0], ["Monotonic Versus Time-of-Day Clocks", ["unreliable-clocks"], 1], ["Clock Synchronization and Accuracy", ["unreliable-clocks"], 1], ["Relying on Synchronized Clocks", ["unreliable-clocks", "clocks"], 1], ["Process Pauses", ["unreliable-clocks"], 1], ["Knowledge, Truth, and Lies", ["fencing-tokens"], 0], ["The Truth Is Defined by the Majority", ["fencing-tokens", "leader-election-locks"], 1], ["Byzantine Faults", ["byzantine-system-models"], 1], ["System Model and Reality", ["byzantine-system-models"], 1]] },
      { n: 9, part: 1, title: "Consistency and Consensus", summary: "Linearizability, causality and total order broadcast, and why distributed transactions and leader election all reduce to consensus.",
        takeaways: ["Linearizability = behaves like a single copy; it has a real cost", "Causal consistency is the strongest model that stays available", "Consensus, total order broadcast and linearizable CAS are equivalent problems"],
        sections: [["Consistency Guarantees", ["consistency-models"], 0], ["Linearizability", ["linearizability"], 0], ["What Makes a System Linearizable?", ["linearizability"], 1], ["Relying on Linearizability", ["linearizability", "leader-election-locks"], 1], ["Implementing Linearizable Systems", ["linearizability"], 1], ["The Cost of Linearizability", ["linearizability", "cap"], 1], ["Ordering Guarantees", ["ordering-causality"], 0], ["Ordering and Causality", ["ordering-causality"], 1], ["Sequence Number Ordering", ["ordering-causality", "clocks", "unique-ids"], 1], ["Total Order Broadcast", ["total-order-broadcast"], 1], ["Distributed Transactions and Consensus", ["consensus"], 0], ["Atomic Commit and Two-Phase Commit (2PC)", ["two-phase-commit"], 1], ["Distributed Transactions in Practice", ["two-phase-commit", "saga"], 1], ["Fault-Tolerant Consensus", ["consensus"], 1], ["Membership and Coordination Services", ["coordination-services", "leader-election-locks", "service-discovery"], 1]] },
      { n: 10, part: 2, title: "Batch Processing", summary: "From Unix pipes to MapReduce and dataflow engines: processing large bounded datasets with joins, immutable outputs and re-runnable tasks.",
        takeaways: ["Immutable inputs + re-runnable tasks = simple fault tolerance", "Pick the join algorithm by data size and layout", "Dataflow engines (Spark/Flink) replaced chained MapReduce"],
        sections: [["Batch Processing with Unix Tools", ["unix-philosophy"], 0], ["Simple Log Analysis", ["unix-philosophy"], 1], ["The Unix Philosophy", ["unix-philosophy"], 1], ["MapReduce and Distributed Filesystems", ["mapreduce"], 0], ["MapReduce Job Execution", ["mapreduce"], 1], ["Reduce-Side Joins and Grouping", ["batch-joins"], 1], ["Map-Side Joins", ["batch-joins"], 1], ["The Output of Batch Workflows", ["batch-joins"], 1], ["Comparing Hadoop to Distributed Databases", ["batch-joins", "oltp-olap"], 1], ["Beyond MapReduce", ["dataflow-engines"], 0], ["Materialization of Intermediate State", ["dataflow-engines"], 1], ["Graphs and Iterative Processing", ["dataflow-engines", "graph-models"], 1], ["High-Level APIs and Languages", ["dataflow-engines", "query-languages"], 1]] },
      { n: 11, part: 2, title: "Stream Processing", summary: "Unbounded event streams: brokers vs logs, keeping systems in sync with CDC and event sourcing, and processing with windows, joins and fault tolerance.",
        takeaways: ["Log-based brokers enable replay and many independent consumers", "A database is a stream of changes plus its current state", "Use event time, and plan for stragglers and effectively-once output"],
        sections: [["Transmitting Event Streams", ["queues-pubsub"], 0], ["Messaging Systems", ["queues-pubsub", "delivery-semantics", "backpressure"], 1], ["Partitioned Logs", ["queues-pubsub"], 1], ["Databases and Streams", ["cdc-outbox"], 0], ["Keeping Systems in Sync", ["cdc-outbox", "derived-data"], 1], ["Change Data Capture", ["cdc-outbox"], 1], ["Event Sourcing", ["event-driven"], 1], ["State, Streams, and Immutability", ["event-driven", "derived-data"], 1], ["Processing Streams", ["batch-stream"], 0], ["Uses of Stream Processing", ["batch-stream", "stream-time-windows", "probabilistic"], 1], ["Reasoning About Time", ["stream-time-windows"], 1], ["Stream Joins", ["stream-joins"], 1], ["Fault Tolerance", ["stream-fault-tolerance", "delivery-semantics"], 1]] },
      { n: 12, part: 2, title: "The Future of Data Systems", summary: "Integrating specialised systems through derived data and logs, unbundling the database, designing for end-to-end correctness, and the ethics of data.",
        takeaways: ["One system of record; everything else is derived via ordered logs", "Correctness needs end-to-end request IDs and integrity checks", "Engineers are responsible for how data affects people"],
        sections: [["Data Integration", ["derived-data"], 0], ["Combining Specialized Tools by Deriving Data", ["derived-data"], 1], ["Batch and Stream Processing", ["derived-data", "batch-stream"], 1], ["Unbundling Databases", ["unbundling"], 0], ["Composing Data Storage Technologies", ["unbundling"], 1], ["Designing Applications Around Dataflow", ["unbundling"], 1], ["Observing Derived State", ["unbundling", "realtime"], 1], ["Aiming for Correctness", ["end-to-end-correctness"], 0], ["The End-to-End Argument for Databases", ["end-to-end-correctness", "delivery-semantics"], 1], ["Enforcing Constraints", ["end-to-end-correctness"], 1], ["Timeliness and Integrity", ["end-to-end-correctness"], 1], ["Trust, but Verify", ["end-to-end-correctness", "merkle-tree"], 1], ["Doing the Right Thing", ["data-ethics"], 0], ["Predictive Analytics", ["data-ethics"], 1], ["Privacy and Tracking", ["data-ethics", "security-basics"], 1]] }
    ]
  };
})();
