/* Second pass over the System Design Primer: section-level details, remaining concepts, appendix material,
 * real-world architectures and study guide. All text is original, written for this cheat sheet. */
window.SD = window.SD || {};
(function () {
  const n = (id, label, sub, type, x, y, desc, icon) => ({ id, label, sub, type, x, y, desc, icon });
  const C = (id) => SD.concepts.find((c) => c.id === id);
  const P = (id, ...pts) => C(id).points.push(...pts);
  const add = (c) => SD.concepts.push(c);

  /* ---------------------------- detail patches ---------------------------- */
  P("latency-throughput", "Rule of thumb: aim for maximal throughput at acceptable latency. Latency = the time for one action; throughput = actions per unit of time.");
  P("consistency-models", "Examples: weak consistency suits memcached-style caches and live media (a dropped second of a call is simply gone); eventual consistency is how DNS and email work (async replication); strong consistency is what file systems and RDBMSs with synchronous replication give you, which suits transactions.");
  P("cap", "In practice: choose CP when the business needs atomic reads and writes (a partitioned node may time out rather than answer). Choose AP when the business can tolerate eventual consistency, or the system must keep working despite external errors.");
  P("dns", "Resolution is cached by browsers, the OS and intermediate resolvers for the record's TTL, so changes propagate with a delay (stale records until the TTL expires).", "Record types in practice: NS (which servers are authoritative for the zone), MX (mail servers), A/AAAA (name → IPv4/IPv6), CNAME (name → another name).", "Managed DNS (Route 53, Cloudflare) can route around servers under maintenance, balance across clusters of different sizes, and split traffic for A/B tests.");
  P("cdn", "Push CDN: you upload content when it changes and rewrite URLs to point at the CDN. Minimal transfer, maximal storage. Suits low-traffic sites or content that rarely changes.", "Pull CDN: the edge fetches from your origin on first request and keeps it for the TTL. Minimal storage, but the first request is slow and files may be re-pulled before they actually changed. Suits high-traffic sites.", "Drawbacks: cost at high traffic (weigh it against origin bandwidth saved), stale content until the TTL expires, and URLs for static content must change to point at the CDN.");
  P("load-balancing", "Benefits: keeps requests away from unhealthy servers, prevents overload, removes single points of failure. Also: SSL/TLS termination (backends skip crypto and certificates live in one place) and session persistence (a cookie pins a client to an instance when the app keeps local sessions).", "Implementation: hardware appliances (fast, expensive) or software (HAProxy, Nginx, Envoy, cloud LBs). Run several in active-passive or active-active so the LB itself isn't a single point of failure.", "L4 balancers look only at IPs and ports and forward packets (often via NAT), so they're cheap and fast. L7 balancers terminate the connection, read headers, cookies and paths, then open a new connection. They can send /video to media servers and /billing to hardened servers.", "Horizontal scaling costs: servers must be clones with no local user state (sessions and uploads go to shared stores), and downstream caches and DBs must handle many more concurrent connections (use connection pooling).", "Drawbacks: a misconfigured or undersized LB becomes the bottleneck, and adding LBs adds complexity.");
  P("proxy-gateway", "Reverse proxy benefits: security (hide backend topology, block IPs, cap connections per client), flexibility (clients see one IP while backends change freely), TLS termination, response compression, response caching, and serving static files (HTML/CSS/JS, images, video) without touching app servers.");
  P("microservices", "Example decomposition for a social image site: profile, follow graph, feed, search and photo-upload services, each with its own data.", "Costs: a different architectural, operational and process mindset compared with a monolith, plus more complex deploys and operations.");
  P("service-discovery", "Registries such as Consul, etcd and ZooKeeper store service names, addresses and ports, run health checks (often an HTTP endpoint) to evict dead instances, and double as a key-value store for shared configuration.");
  P("replication", "Primary-replica specifics: replicas can themselves feed further replicas (a tree). If the primary dies, the system can keep serving reads in read-only mode until a replica is promoted, which needs extra promotion logic.", "Replication drawbacks: writes not yet replicated are lost if the primary dies; replicas must replay every write, so write-heavy loads bog them down; more replicas mean more replication lag; replicas often apply changes single-threaded while the primary writes in parallel; and more hardware and complexity.");
  P("multi-leader", "Multi-primary (master-master) specifics: either a load balancer or application logic must decide where each write goes. Most multi-primary setups are either loosely consistent (breaking ACID) or pay extra write latency to synchronise. Conflict resolution gets harder as you add write nodes and latency grows.");
  P("sharding", "Common shard keys for users: last-name initial (simple, but skewed) or geography (data locality). Benefits beyond capacity: smaller indexes (faster queries), more cache hits, fault isolation (one shard down ≠ all down, but replicate each shard), and no single write primary.", "Costs: application logic must be shard-aware, power users make shards lopsided (consistent hashing reduces the data moved during rebalancing), cross-shard joins are hard, and there's more hardware to run.");
  P("normalization", "Many databases maintain denormalised copies for you via materialised views (PostgreSQL, Oracle). With read:write ratios of 100:1 to 1000:1, removing expensive joins from reads often pays off, especially once data is federated or sharded and joins would cross machines.", "Costs: duplicated data, constraints needed to keep copies in sync, and under heavy write load a denormalised schema can perform worse than a normalised one.");
  P("sql-tuning", "Schema tightening (MySQL-style advice): CHAR for fixed-length fields (fast random access vs scanning for the end of a VARCHAR); TEXT for large bodies such as posts (stored out of line via a pointer); INT for values up to ~4 billion; DECIMAL for currency (no floating-point errors); VARCHAR(255) is the most characters countable in one byte in some engines; keep large BLOBs out of the DB and store their location instead.", "Indexing notes: indexes are usually self-balancing B-trees (logarithmic search, insert, delete and ordered scans); they occupy memory and slow writes; for big bulk loads it can be faster to drop indexes, load, then rebuild.", "Partition tables by moving hot rows or columns into a separate table so they stay in memory. Tune (or disable) the query cache, since in some engines it causes contention.");
  P("caching", "Why a cache in front of a DB helps even when the DB is fast: popular items skew reads towards a few partitions. A cache absorbs uneven load and traffic spikes.", "Cache-aside costs: a miss costs three trips (cache, DB, cache set); entries go stale unless you use a TTL or also write through; a failed node is replaced by an empty one, so latency rises while it warms. It's the typical Memcached pattern.", "Write-through costs: slow writes (users tolerate slow updates better than slow reads); a new or replacement node stays cold until entries are rewritten (combine with cache-aside); much of what's written is never read (use a TTL).", "Write-behind costs: data loss if the cache dies before flushing, and it's more complex to implement than cache-aside or write-through.", "Overall cache costs: you must keep caches consistent with the source of truth (invalidation is genuinely hard), and the app needs new code paths and infrastructure (Redis/Memcached).");
  P("cache-layers", "Cache granularity: row level, query level, fully formed serialisable objects, and fully rendered HTML fragments or pages.", "Good candidates for object caching: user sessions, fully rendered pages, activity streams, and user graph data (friend lists).", "Avoid file-based caching on app servers. It breaks the 'servers are identical clones' rule and complicates autoscaling.", "Redis vs Memcached for the application tier: Redis adds persistence options and built-in structures (sorted sets, lists); both typically evict with LRU.");
  P("queues-pubsub", "UX pattern: enqueue the slow job, tell the user it's in progress, and optionally fake completion locally (your post shows on your own timeline immediately while fan-out to followers happens in the background).", "Broker trade-offs: Redis works as a lightweight broker but can lose messages; RabbitMQ is feature-rich but speaks AMQP and you operate the nodes; Amazon SQS is fully managed but has higher latency and may deliver a message more than once.");
  P("backpressure", "If a queue grows beyond memory, it spills to disk and everything slows. Cap the queue size; when it's full, reject new work (HTTP 503 'server busy') and have clients retry later with exponential backoff.", "Not everything should be async: cheap computations and real-time interactions are often better done synchronously, because queues add delay and complexity.");
  P("task-queues", "Celery (Python) is the classic example with built-in scheduling; equivalents include Sidekiq (Ruby), BullMQ (Node) and cloud task services.", "Asynchronism also means doing work ahead of time: periodic aggregation, pre-rendering pages, and warming caches, so requests find results ready.");
  P("network-protocols", "TCP details: sequence numbers and checksums detect loss and corruption; unacknowledged segments are resent and repeated timeouts drop the connection. Many open connections cost memory (for example web workers ↔ memcached), so use connection pooling, or UDP where appropriate. Typical TCP users: web, databases, SMTP, FTP, SSH.", "UDP details: datagram-level guarantees only (packets may arrive out of order or not at all), no congestion control, and it can broadcast to a whole subnet, which is how DHCP works before a client even has an IP. Choose it when you need the lowest latency, late data is worse than lost data, or you'll implement your own error correction.");
  P("security-basics", "The primer's minimum bar: encrypt in transit and at rest; sanitise every user-controlled input (XSS, SQL injection); always use parameterised queries; apply the principle of least privilege.");
  P("performance-vs-scalability", "Scalability can mean serving more units of work or handling larger units of work (growing datasets), not just more users.");
  P("availability", "Downtime per day: 99.9% ≈ 1 min 26 s; 99.99% ≈ 8.6 s. Per year: 99.9% ≈ 8 h 46 min; 99.99% ≈ 52.6 min.");

  const dbPatch = { keyvalue: " Abstraction: a hash table, O(1) reads and writes, often memory- or SSD-backed; some keep keys sorted for range scans and store metadata with values. The base for document and some graph stores.", document: " Abstraction: a key-value store whose values are documents (JSON/XML/BSON) that can be queried by internal structure; grouped by collections, tags or directories.", widecolumn: " Abstraction: a nested map ColumnFamily<RowKey, Columns<ColKey, Value, Timestamp>>. Columns are grouped into families, and each value carries a timestamp for versioning and conflict resolution. Lineage: Bigtable → HBase, Cassandra.", graph: " Abstraction: a graph. Each node is a record and each arc a relationship, optimised for many-to-many links. Tooling is younger; some are accessed via REST APIs." };
  Object.entries(dbPatch).forEach(([id, t]) => { const d = SD.databases.find((x) => x.id === id); d.model += t; });

  /* ------------------------------ new concepts ------------------------------ */
  add({ id: "scalability-basics", cat: "Fundamentals", title: "Scalability Basics: Clones, Databases, Caches, Async",
    summary: "The 'start here' mental model: clone stateless servers behind a load balancer, fix the database, cache aggressively, and move slow work out of the request.",
    points: ["Vertical scaling (a bigger box) is simplest but hits hardware and cost ceilings. Horizontal scaling adds identical machines behind a load balancer.", "Clones: every app server runs the same code from the same image and keeps no user state. Sessions go to a shared store (Redis or a DB) and uploads to object storage, so any server can handle any request and servers can come and go freely.", "Databases: when the DB becomes the bottleneck, either keep it relational and scale it (replication for reads, partitioning or sharding, denormalisation, tuning, at growing cost), or move hot paths to a denormalised NoSQL store with joins in application code. Add a cache either way.", "Replication for reads (primary + replicas) and partitioning (by user, feature or geography) are the two core database scaling moves.", "Caches: put an in-memory cache between the app and the DB. Prefer caching assembled objects over raw query results, which are hard to invalidate.", "Asynchronism: do work before it's asked for (pre-render pages, pre-aggregate reports and serve them statically), and push slow work to queues and workers, telling the user it's in progress."],
    use: ["The first 10 minutes of almost any design: stateless tier + LB + DB + cache + queue"], pitfalls: ["Sticky local state on servers", "Scaling the web tier while the DB is the bottleneck"] });
  add({ id: "sql-or-nosql", cat: "Data & Storage", title: "SQL or NoSQL? A Decision Checklist",
    summary: "Default to SQL for structured, relational, transactional data. Reach for NoSQL when the data shape, scale or throughput demands it.",
    points: ["NoSQL stores (key-value, document, wide-column, graph) usually denormalise, do joins in application code, lack full ACID transactions and favour eventual consistency (BASE).", "BASE: Basically Available (the system stays up), Soft state (state may change without new input as replicas converge), Eventual consistency (it settles if writes stop). Relative to CAP, BASE chooses availability.", "Good fits for NoSQL: rapid ingest of clickstream and log data, leaderboards and scores, temporary data such as shopping carts, very hot tables, and metadata or lookup tables.", "Many real systems use both: SQL as the system of record, and NoSQL for specific high-volume or simple-access paths."],
    use: ["Justifying the database choice in an interview (then use the Advisor for specifics)"], pitfalls: ["Choosing NoSQL for 'scale' when the data is highly relational and small", "Assuming NoSQL means no schema design (access patterns are the schema)"],
    table: [["Factor", "Lean SQL", "Lean NoSQL"], ["Data", "Structured, relational", "Semi-structured, non-relational"], ["Schema", "Strict, enforced", "Dynamic / flexible"], ["Joins", "Complex joins needed", "Few or none"], ["Transactions", "Required", "Rarely needed"], ["Scale", "Clear, well-known scaling patterns", "Many TB–PB, very high IOPS, data-intensive"], ["Ecosystem", "Very mature: talent, tools, community", "Varies by product"], ["Lookups", "Indexed lookups are very fast", "Key lookups at massive scale"]] });
  add({ id: "http-methods", cat: "Networking & Traffic", title: "HTTP Methods, Status Codes & Caching Semantics",
    summary: "HTTP is a self-contained request/response protocol: verb + resource + headers. Its semantics (safety, idempotency, cacheability) let proxies, caches and retries work correctly.",
    points: ["Requests and responses carry everything needed, so they can pass through load balancers, caches, TLS terminators and compressors along the way.", "Safe = no side effects (reads). Idempotent = repeating it has the same effect as doing it once, so it's safe to retry. That's why retries on POST need idempotency keys.", "Status code classes: 1xx informational, 2xx success (200 OK, 201 Created, 202 Accepted for async work, 204 No Content), 3xx redirection (301/302/307/308, 304 Not Modified), 4xx client errors (400, 401, 403, 404, 409 Conflict, 429 Too Many Requests), 5xx server errors (500, 502 Bad Gateway, 503 Unavailable, 504 Gateway Timeout).", "Caching headers: Cache-Control (max-age, no-store, private/public), ETag + If-None-Match or Last-Modified + If-Modified-Since for revalidation (304), and Vary for representation-dependent caching.", "Versions: HTTP/1.1 keep-alive connections; HTTP/2 multiplexes many streams over one TCP connection; HTTP/3 runs on QUIC over UDP to avoid TCP head-of-line blocking.", "HTTP sits at the application layer, on top of TCP (HTTP/1–2) or UDP via QUIC (HTTP/3)."],
    use: ["API design, retry policies, CDN and browser caching"], pitfalls: ["Side effects in GET handlers (crawlers and prefetchers will trigger them)", "Returning 200 with an error body"],
    table: [["Method", "Typical use", "Safe", "Idempotent", "Cacheable"], ["GET", "Read a resource", "Yes", "Yes", "Yes"], ["HEAD", "Headers only", "Yes", "Yes", "Yes"], ["POST", "Create / trigger processing", "No", "No", "Only with explicit freshness info"], ["PUT", "Create or replace a resource", "No", "Yes", "No"], ["PATCH", "Partial update", "No", "Not guaranteed", "Only with explicit freshness info"], ["DELETE", "Delete a resource", "No", "Yes", "No"], ["OPTIONS", "Capabilities / CORS preflight", "Yes", "Yes", "No"]] });
  add({ id: "rest-vs-rpc", cat: "Networking & Traffic", title: "RPC vs REST in Depth",
    summary: "RPC exposes behaviours (call a function remotely); REST exposes data (act on resources with uniform verbs). Pick by coupling, audience and performance needs.",
    points: ["RPC mechanics: the client calls a local stub, the stub marshals the procedure ID and arguments into a message, the OS/network carries it, the server stub unmarshals it and invokes the real procedure, and the reply travels back the same way. Frameworks: gRPC/Protobuf, Thrift, Avro RPC, Finagle.", "Because remote calls are slower and less reliable than local ones, make RPC calls visibly different from local calls in code (explicit timeouts, retries, error types).", "Prefer a native client library (SDK) / RPC when you control the target platform, want to control how logic and errors are exposed, and performance and user experience are paramount, typically for internal service-to-service calls.", "RPC drawbacks: clients couple tightly to the service implementation, every new operation needs a new endpoint, it's harder to debug, and generic infrastructure (HTTP caches such as Squid, browser tooling) may not work out of the box.", "REST qualities: identify resources with URIs (the same URI regardless of operation); change them through representations using verbs, headers and bodies; self-descriptive errors via standard status codes; and hypermedia (HATEOAS): responses link to related actions so the API is navigable. Everything is stateless and cacheable, which suits horizontal scaling.", "REST drawbacks: awkward for operations that aren't a simple resource hierarchy (e.g. 'all records updated in the last hour matching these events' ends up as path + query + body); the verb set may not fit actions like 'archive expired documents'; nested views need several round trips (bad on mobile networks, which is where GraphQL and BFFs help); and responses tend to bloat as fields are added."],
    use: ["Public APIs → REST; internal high-performance calls → RPC (gRPC)"], pitfalls: ["RPC endpoints that are really CRUD in disguise (/getUser, /updateUser…)", "REST APIs that tunnel everything through POST"],
    table: [["Operation", "RPC style", "REST style"], ["Create an account", "POST /createAccount", "POST /accounts"], ["Close an account", "POST /closeAccount {id: 42}", "DELETE /accounts/42"], ["Fetch an account", "GET /getAccount?id=42", "GET /accounts/42"], ["List an account's orders", "GET /listOrders?accountId=42", "GET /accounts/42/orders"], ["Add an order", "POST /addOrder {accountId: 42, sku}", "POST /accounts/42/orders {sku}"], ["Change an order", "POST /updateOrder {orderId: 7, qty: 2}", "PATCH /orders/7 {qty: 2}"], ["Cancel an order", "POST /cancelOrder {orderId: 7}", "DELETE /orders/7 (or POST /orders/7/cancellation)"]] });
  add({ id: "osi-model", cat: "Networking & Traffic", title: "OSI Model & Network Layers",
    summary: "A seven-layer vocabulary for networking. In system design it mostly tells you what a component can see: an L4 device sees ports, an L7 device sees HTTP.",
    points: ["The Internet actually runs the simpler TCP/IP model (link, internet, transport, application), but 'L4' and 'L7' come from OSI and are used everywhere.", "L3 (network, IP) handles routing between networks, including BGP and anycast. L4 (transport, TCP/UDP/QUIC) deals with ports and connections. L7 (application: HTTP, gRPC, DNS, SMTP) deals with message content.", "L4 load balancers and firewalls route on IP and port without reading payloads, which is fast and protocol-agnostic. L7 load balancers, API gateways and WAFs parse requests to route by path, header or cookie and can terminate TLS.", "TLS logically sits between transport and application (OSI's presentation layer also covers encoding such as JSON or Protobuf, and compression).", "Debugging mindset: work up the stack. Is there a physical link? IP reachability? Is the port open (TCP handshake)? Is the TLS handshake OK? Is the HTTP response OK?"],
    use: ["Explaining L4 vs L7 load balancing, where TLS terminates, what a CDN or WAF can inspect"], pitfalls: ["Expecting an L4 balancer to route by URL path"],
    table: [["#", "Layer", "Unit", "Examples", "Design relevance"], ["7", "Application", "Message", "HTTP, gRPC, DNS, SMTP, WebSocket", "L7 LB, API gateway, WAF, CDN"], ["6", "Presentation", "Message", "TLS, JSON/Protobuf, compression", "Encryption & encoding"], ["5", "Session", "Message", "Session setup, RPC sessions", "Connection/session semantics"], ["4", "Transport", "Segment / datagram", "TCP, UDP, QUIC", "L4 LB, ports, connection pools"], ["3", "Network", "Packet", "IP, ICMP, BGP", "Routing, anycast, VPCs"], ["2", "Data link", "Frame", "Ethernet, Wi-Fi, ARP", "Switches, MAC addresses"], ["1", "Physical", "Bits", "Fibre, copper, radio", "Cabling, NICs"]] });
  add({ id: "scatter-gather", cat: "Distributed Systems", title: "Scatter-Gather",
    summary: "Fan a request out to many workers or shards in parallel, then gather and merge their partial results. It's how search engines and sharded queries answer in milliseconds.",
    points: ["An aggregator (root) sends the query to every relevant shard (scatter). Each returns a partial result, such as its local top-k, and the aggregator merges them into the final answer (gather).", "Latency is set by the slowest shard (tail latency amplification): with 100 shards, even a p99 blip on one shard slows most requests.", "Tail-tolerance tricks: per-shard deadlines with partial results, hedged or backup requests to replicas, replicas per shard, and multi-level aggregator trees to limit fan-in.", "It shows up in search engines, local (document-partitioned) secondary indexes, MPP analytics, microservice composition (BFF/API gateway aggregation) and map-reduce style reductions.", "Merging must be associative: top-k lists, sums and counts, and sketches (HyperLogLog, Count-Min) merge cleanly."],
    use: ["Queries that can't be routed to one partition"], pitfalls: ["Waiting indefinitely for every shard", "Fan-out to thousands of shards per request without an aggregation tree"],
    diagram: { nodes: [
      n("c", "Client", "", "client", 80, 260, "Sends one query."),
      n("agg", "Aggregator", "scatter · merge", "service", 330, 260, "Fans out, applies a deadline, merges partial results."),
      n("s1", "Shard 1", "", "search", 640, 60, "Returns its local top-k."),
      n("s2", "Shard 2", "", "search", 640, 190, "Returns its local top-k."),
      n("s3", "Shard 3", "", "search", 640, 330, "Returns its local top-k."),
      n("s4", "Shard 4", "slow!", "search", 640, 460, "The tail-latency culprit.")
    ], edges: [["c", "agg"], ["agg", "s1"], ["agg", "s2"], ["agg", "s3"], ["agg", "s4"]], flows: [{ name: "Scatter → gather", steps: [
      ["c", "agg", "search('kafka'), deadline 100 ms"],
      ["agg", "s1", "Scatter to every shard in parallel"], ["agg", "s2", "…"], ["agg", "s3", "…"], ["agg", "s4", "…"],
      ["s1", "agg", "Top 10 from shard 1"], ["s2", "agg", "Top 10 from shard 2"], ["s3", "agg", "Top 10 from shard 3"],
      ["agg", "s4", "Shard 4 misses the deadline → hedge to its replica, or return a partial result"],
      ["agg", "c", "Merge 3–4 lists → global top 10"]
    ]}]} });

  /* ------------------------------ numbers ------------------------------ */
  SD.latencyClassic = [
    ["L1 cache reference", "0.5 ns"], ["Branch mispredict", "5 ns"], ["L2 cache reference", "7 ns"], ["Mutex lock/unlock", "25 ns"], ["Main memory reference", "100 ns"],
    ["Compress 1 KB with Zippy", "10 µs"], ["Send 1 KB over 1 Gbps network", "10 µs"], ["Read 4 KB randomly from SSD", "150 µs"], ["Read 1 MB sequentially from memory", "250 µs"],
    ["Round trip within same datacenter", "500 µs"], ["Read 1 MB sequentially from SSD", "1 ms"], ["HDD seek", "10 ms"], ["Read 1 MB sequentially over 1 Gbps", "10 ms"],
    ["Read 1 MB sequentially from HDD", "30 ms"], ["Packet CA → Netherlands → CA", "150 ms"]
  ];
  SD.handyMetrics = [
    ["Sequential read from HDD", "~30 MB/s"], ["Sequential read over 1 Gbps Ethernet", "~100 MB/s"], ["Sequential read from SSD", "~1 GB/s"], ["Sequential read from main memory", "~4 GB/s"],
    ["World-wide round trips", "~6–7 per second"], ["Round trips within a datacenter", "~2,000 per second"]
  ];
  SD.nines = [
    ["99%", "two nines", "3.65 days", "7.3 hours", "1.68 hours", "14.4 min"],
    ["99.9%", "three nines", "8.77 hours", "43.8 min", "10.1 min", "1.44 min"],
    ["99.95%", "three and a half", "4.38 hours", "21.9 min", "5.04 min", "43.2 s"],
    ["99.99%", "four nines", "52.6 min", "4.38 min", "1.01 min", "8.64 s"],
    ["99.999%", "five nines", "5.26 min", "26.3 s", "6.05 s", "0.86 s"],
    ["99.9999%", "six nines", "31.6 s", "2.63 s", "0.6 s", "86 ms"]
  ];

  /* ------------------------------ study guide ------------------------------ */
  SD.studyPlan = {
    note: "You don't need to know everything. Expectations depend on your experience, your background, the role (architects and leads are expected to go deeper) and the company. Start broad, then go deep in a few areas.",
    rows: [
      ["Read the topics (Concepts + Databases) for a broad understanding", "✓", "✓", "✓"],
      ["Read a few engineering-blog articles from companies you're targeting", "✓", "✓", "✓"],
      ["Read a few real-world architectures", "✓", "✓", "✓"],
      ["Review how to approach a design question (this page)", "✓", "✓", "✓"],
      ["Work through system design scenarios", "Some", "Many", "Most"],
      ["Work through object-oriented design questions", "Some", "Many", "Most"],
      ["Review additional interview questions", "Some", "Many", "Most"]
    ],
    timelines: [["Short", "Breadth across topics; solve some questions"], ["Medium", "Breadth + some depth; solve many questions"], ["Long", "Breadth + more depth; solve most questions"]]
  };
  const req = SD.framework.find((f) => f.step === "Clarify requirements");
  req.items.push("What are the inputs and outputs of the system? How will users actually use it?");
  const dd = SD.framework.find((f) => f.step === "Deep dives");
  dd.items.push("Example (URL shortener): hashing the long URL (MD5 → Base62), handling collisions, SQL vs NoSQL, schema, lookup path, API and object design");
  const hl = SD.framework.find((f) => f.step === "High-level design");
  hl.items.push("Justify each component; then scale it: do you need a load balancer, horizontal scaling, caching, sharding? Everything is a trade-off.");

  /* ------------------------ real-world systems & companies ------------------------ */
  SD.systems = [
    ["Data processing", "MapReduce", "Google's batch model: map, shuffle, reduce, with re-runnable tasks.", ["mapreduce"]],
    ["Data processing", "Spark", "In-memory dataflow engine with lineage-based recovery (RDDs); batch, SQL, streaming and ML.", ["dataflow-engines"]],
    ["Data processing", "Storm", "Twitter's early real-time stream processor (spouts and bolts topologies).", ["batch-stream", "stream-fault-tolerance"]],
    ["Data store", "Bigtable", "Google's sparse, sorted, wide-column store on GFS with SSTables.", ["d:widecolumn", "storage-engines"]],
    ["Data store", "HBase", "Open-source Bigtable on HDFS, coordinated by ZooKeeper.", ["d:widecolumn", "coordination-services"]],
    ["Data store", "Cassandra", "Masterless wide-column store: Dynamo-style ring plus the Bigtable data model.", ["d:widecolumn", "consistent-hashing", "quorum"]],
    ["Data store", "DynamoDB", "Amazon's managed key-value/document store descended from Dynamo ideas.", ["d:keyvalue", "s:kv-store"]],
    ["Data store", "MongoDB", "Document database with replica sets and range/hash sharding.", ["d:document", "sharding"]],
    ["Data store", "Spanner", "Globally distributed SQL with TrueTime and external consistency.", ["d:newsql", "unreliable-clocks"]],
    ["Data store", "Memcached", "Simple, multi-threaded, in-memory cache with LRU and client-side hashing.", ["d:inmemory", "s:distributed-cache"]],
    ["Data store", "Redis", "In-memory data-structure server with persistence, replication and clustering.", ["d:inmemory", "s:leaderboard"]],
    ["File system", "GFS", "Google File System: large chunks, a single master, replicated chunk servers.", ["mapreduce"]],
    ["File system", "HDFS", "Open-source GFS: NameNode + DataNodes, replicated blocks.", ["mapreduce"]],
    ["Misc", "Chubby", "Paxos-based lock service and small-file store for coarse-grained coordination.", ["coordination-services"]],
    ["Misc", "Dapper", "Google's distributed tracing: trace and span IDs propagated across services.", ["observability"]],
    ["Misc", "Kafka", "Partitioned, replicated commit log used for pub/sub and streaming.", ["queues-pubsub"]],
    ["Misc", "ZooKeeper", "Consensus-backed coordination: config, naming, locks, membership.", ["coordination-services", "leader-election-locks"]]
  ];
  SD.companies = [
    ["Amazon", "Service-oriented architecture with small teams owning services and their data; Dynamo for the always-writable shopping cart.", ["microservices", "s:kv-store"]],
    ["Cinchcast", "A pipeline producing ~1,500 hours of audio per day: media processing via queues and workers.", ["task-queues", "s:video-streaming"]],
    ["DataSift", "Real-time data mining of ~120 K tweets/s: a large-scale stream-processing and filtering pipeline.", ["batch-stream", "s:trending-topics"]],
    ["Dropbox", "Split metadata (sharded MySQL) from block storage (content-addressed chunks), later built its own storage system.", ["s:file-sync"]],
    ["ESPN", "Live scores at ~100 K requests/s: heavy caching, CDN and push for spiky live events.", ["cache-layers", "cdn"]],
    ["Google", "Commodity hardware plus software fault tolerance: GFS, MapReduce, Bigtable, Spanner, Borg.", ["reliability", "mapreduce"]],
    ["Instagram", "A tiny team: Django + sharded PostgreSQL (IDs embed the shard), Redis, Memcached, and object storage + CDN for photos.", ["s:photo-sharing", "unique-ids"]],
    ["Justin.tv", "Live video broadcasting: ingest, transcoding and edge distribution for live streams.", ["s:video-streaming", "cdn"]],
    ["Facebook", "A massive Memcache tier with leases and invalidation, TAO for the social graph, and Haystack for photos.", ["s:distributed-cache", "s:social-graph"]],
    ["Flickr", "Federated/sharded MySQL with users pinned to shards, master-master pairs, and ticket servers for IDs.", ["federation", "sharding", "unique-ids"]],
    ["Mailbox", "0 → 1 M users in 6 weeks: a signup waitlist to meter load while scaling the backend.", ["backpressure", "scaling"]],
    ["Netflix", "Control plane of microservices on AWS; video bytes served from its own ISP-embedded CDN (Open Connect); chaos engineering.", ["s:video-streaming", "reliability"]],
    ["Pinterest", "Grew to billions of page views with a small team by keeping tech simple: sharded MySQL, Redis, Memcached.", ["sharding", "caching"]],
    ["Playfish", "Social games for ~50 M monthly users, built on elastic cloud infrastructure.", ["s:multiplayer-game", "s:scaling-aws"]],
    ["PlentyOfFish", "Famously run by a handful of people on a heavily scaled-up stack: vertical scaling goes further than you think.", ["scaling", "performance-vs-scalability"]],
    ["Salesforce", "~1.3 B transactions/day on a multi-tenant architecture partitioned into pods, with tenant isolation by org ID.", ["sharding", "multi-region"]],
    ["Stack Overflow", "A deliberately monolithic app on a small number of powerful servers with SQL Server and Redis caching.", ["microservices", "scaling"]],
    ["TripAdvisor", "~200 M dynamic page views over ~30 TB of data: services, caching and careful database scaling.", ["cache-layers", "sharding"]],
    ["Tumblr", "~15 B page views/month; moved from a monolith towards services and a central event firehose.", ["microservices", "queues-pubsub"]],
    ["Twitter", "From a Rails monolith to JVM services; fan-out timelines in Redis; sharded MySQL for tweets; Snowflake IDs.", ["s:news-feed", "unique-ids"]],
    ["Uber", "Real-time dispatch with geospatial indexing and consistent-hash rings; later a cautionary tale of microservice sprawl.", ["s:ride-sharing", "microservices"]],
    ["WhatsApp", "Erlang on FreeBSD with very few engineers and millions of connections per server.", ["s:chat"]],
    ["YouTube", "Python app tier; MySQL scaled through replication and then sharding (which led to Vitess); CDN for video.", ["s:video-streaming", "sharding"]]
  ];
  SD.blogs = [["Airbnb Engineering", "http://nerds.airbnb.com/"], ["Atlassian Developers", "https://developer.atlassian.com/blog/"], ["AWS Blog", "https://aws.amazon.com/blogs/aws/"], ["Bitly Engineering Blog", "http://word.bitly.com/"], ["Box Blogs", "https://blog.box.com/blog/category/engineering"], ["Cloudera Developer Blog", "http://blog.cloudera.com/"], ["Dropbox Tech Blog", "https://tech.dropbox.com/"], ["Engineering at Quora", "https://www.quora.com/q/quoraengineering"], ["Ebay Tech Blog", "http://www.ebaytechblog.com/"], ["Evernote Tech Blog", "https://blog.evernote.com/tech/"], ["Etsy Code as Craft", "http://codeascraft.com/"], ["Facebook Engineering", "https://www.facebook.com/Engineering"], ["Flickr Code", "http://code.flickr.net/"], ["Foursquare Engineering Blog", "http://engineering.foursquare.com/"], ["GitHub Engineering Blog", "https://github.blog/category/engineering"], ["Google Research Blog", "http://googleresearch.blogspot.com/"], ["Groupon Engineering Blog", "https://engineering.groupon.com/"], ["Heroku Engineering Blog", "https://engineering.heroku.com/"], ["Hubspot Engineering Blog", "http://product.hubspot.com/blog/topic/engineering"], ["High Scalability", "http://highscalability.com/"], ["Instagram Engineering", "http://instagram-engineering.tumblr.com/"], ["Intel Software Blog", "https://software.intel.com/en-us/blogs/"], ["Jane Street Tech Blog", "https://blogs.janestreet.com/category/ocaml/"], ["LinkedIn Engineering", "http://engineering.linkedin.com/blog"], ["Microsoft Engineering", "https://engineering.microsoft.com/"], ["Microsoft Python Engineering", "https://blogs.msdn.microsoft.com/pythonengineering/"], ["Netflix Tech Blog", "http://techblog.netflix.com/"], ["Paypal Developer Blog", "https://developer.paypal.com/community/blog/"], ["Pinterest Engineering Blog", "https://medium.com/@Pinterest_Engineering"], ["Reddit Blog", "http://www.redditblog.com/"], ["Salesforce Engineering Blog", "https://developer.salesforce.com/blogs/engineering/"], ["Slack Engineering Blog", "https://slack.engineering/"], ["Spotify Labs", "https://labs.spotify.com/"], ["Stripe Engineering Blog", "https://stripe.com/blog/engineering"], ["Twilio Engineering Blog", "http://www.twilio.com/engineering"], ["Twitter Engineering", "https://blog.twitter.com/engineering/"], ["Uber Engineering Blog", "http://eng.uber.com/"], ["Yahoo Engineering Blog", "http://yahooeng.tumblr.com/"], ["Yelp Engineering Blog", "http://engineeringblog.yelp.com/"], ["Zynga Engineering Blog", "https://www.zynga.com/blogs/engineering"], ["kilimchoi/engineering-blogs (big list)", "https://github.com/kilimchoi/engineering-blogs"]];

  /* ------------------------------ primer map: extra groups ------------------------------ */
  const G = SD.primer.groups;
  G.unshift(
    { title: "Start here", items: [["Scalability lecture: vertical & horizontal scaling", ["scaling", "scalability-basics"]], ["Scalability lecture: caching, load balancing", ["caching", "load-balancing"]], ["Scalability lecture: database replication & partitioning", ["replication", "sharding"]], ["Scalability article: clones", ["scalability-basics"]], ["Scalability article: databases", ["scalability-basics", "sql-or-nosql"]], ["Scalability article: caches", ["cache-layers", "caching"]], ["Scalability article: asynchronism", ["scalability-basics", "task-queues"]], ["Next steps: everything is a trade-off", ["p:framework"]]] },
    { title: "Study guide & approach", items: [["Study guide by timeline (short / medium / long)", ["p:framework"]], ["Step 1: outline use cases, constraints, assumptions", ["p:framework"]], ["Step 2: create a high-level design", ["p:framework"]], ["Step 3: design core components", ["p:framework", "s:url-shortener"]], ["Step 4: scale the design", ["p:framework", "scalability-basics"]], ["Back-of-the-envelope calculations", ["back-of-envelope", "p:numbers"]], ["Anki flashcards", ["p:quiz"]]] }
  );
  const find = (t) => G.find((g) => g.title === t);
  find("Fundamentals").items.push(["Disadvantages of fail-over; availability per day", ["ha-dr", "availability"]]);
  find("Traffic").items.push(["DNS records (NS, MX, A, CNAME), managed DNS & routing policies, DNS disadvantages", ["dns"]], ["CDN disadvantages (cost, staleness, URL rewrites)", ["cdn"]], ["LB benefits: SSL termination, session persistence; L4 NAT vs L7 content routing", ["load-balancing", "osi-model"]], ["Horizontal scaling disadvantages; load balancer disadvantages", ["load-balancing", "scalability-basics"]], ["Reverse proxy benefits & disadvantages", ["proxy-gateway"]], ["Application layer disadvantages", ["microservices"]]);
  find("Database").items.push(["RDBMS & ACID", ["acid-base", "d:relational"]], ["Replication disadvantages (both styles)", ["replication", "multi-leader"]], ["Federation / sharding / denormalization disadvantages", ["federation", "sharding", "normalization"]], ["SQL tuning: tighten schema, indices, joins, partition tables, query cache", ["sql-tuning"]], ["NoSQL & BASE", ["sql-or-nosql", "acid-base"]], ["SQL or NoSQL: reasons for each, sample NoSQL data", ["sql-or-nosql"]]);
  find("Cache").items.push(["Row-level, query-level, serialisable objects, rendered HTML; avoid file-based caching", ["cache-layers"]], ["Disadvantages of each update strategy and of caching overall", ["caching", "cache-problems"]]);
  find("Asynchronism & communication").items.push(["Brokers: Redis, RabbitMQ, SQS trade-offs", ["queues-pubsub"]], ["Asynchronism disadvantages (when to stay synchronous)", ["backpressure"]], ["HTTP verbs: idempotent, safe, cacheable", ["http-methods"]], ["TCP details: handshake, checksums, connection pooling; UDP broadcast/DHCP", ["network-protocols", "osi-model"]], ["RPC internals (stubs, marshalling), when to use native SDKs, RPC disadvantages", ["rest-vs-rpc"]], ["REST qualities (URIs, verbs, status codes, HATEOAS) & disadvantages", ["rest-vs-rpc", "http-methods"]], ["RPC vs REST calls comparison", ["rest-vs-rpc"]], ["Security basics: encryption, input sanitisation (XSS, SQLi), parameterised queries, least privilege", ["security-basics"]]);
  find("Appendix").items.push(["Latency numbers visualised & handy throughput metrics", ["p:numbers"]], ["Real world architectures", ["p:map"]], ["Company architectures", ["p:map"]], ["Company engineering blogs", ["p:map"]]);
  G.push({ title: "Under development (primer roadmap)", items: [["Distributed computing with MapReduce", ["mapreduce", "batch-joins"]], ["Consistent hashing", ["consistent-hashing"]], ["Scatter gather", ["scatter-gather"]]] });

  /* ------------------------------ glossary & quiz ------------------------------ */
  SD.glossary.push(
    ["HATEOAS", "Hypermedia as the engine of application state: REST responses link to related resources and actions."],
    ["Idempotent (HTTP)", "Repeating the request has the same effect as sending it once (GET, PUT, DELETE)."],
    ["Safe method (HTTP)", "A request with no side effects on the server (GET, HEAD, OPTIONS)."],
    ["Client stub", "RPC code that marshals a local call into a network request."],
    ["Marshalling", "Packing a procedure ID and arguments into a message for transmission."],
    ["Scatter-gather", "Fan a request out to many shards in parallel, then merge the partial results."],
    ["SSL/TLS termination", "Decrypting at a load balancer or proxy so backends don't handle encryption."],
    ["Session persistence", "Routing a client to the same backend (usually via a cookie)."],
    ["NAT", "Network address translation: rewriting packet addresses, used by L4 load balancers."],
    ["Push CDN / pull CDN", "You upload content to the CDN vs the CDN fetches from your origin on demand."],
    ["Hot standby / cold standby", "A passive server already running vs one that must boot before taking over."],
    ["Connection pooling", "Reusing a set of open connections instead of opening one per request."],
    ["OSI model", "Seven-layer model of networking; L4 = transport, L7 = application."]
  );
  SD.quiz.push(
    { q: "Which HTTP method is idempotent but NOT safe?", o: ["GET", "PUT", "POST", "HEAD"], a: 1, why: "PUT changes server state (not safe), but repeating the same PUT leaves the same result (idempotent). DELETE is the same." },
    { q: "Your app servers keep user sessions in local memory, blocking autoscaling. The fix:", o: ["Sticky sessions forever", "Move sessions to a shared store (Redis/DB) so servers are identical clones", "Bigger servers", "Longer TTLs"], a: 1, why: "Stateless 'clone' servers let a load balancer send any request anywhere and let instances come and go." },
    { q: "A load balancer that routes /video to media servers by URL path must be…", o: ["Layer 3", "Layer 4", "Layer 7", "A DNS server"], a: 2, why: "Only an L7 balancer reads the HTTP request (path, headers, cookies). L4 sees just IPs and ports." },
    { q: "Site with heavy traffic and frequently changing content: push or pull CDN?", o: ["Push", "Pull", "Neither", "Both are identical"], a: 1, why: "A pull CDN fetches on demand and keeps only recently requested content, spreading heavy traffic efficiently." },
    { q: "Main drawback of cache-aside on a miss?", o: ["Data is never stale", "Three trips (cache, DB, cache set), adding latency", "Writes become slower", "It can't use a TTL"], a: 1, why: "Each miss checks the cache, reads the DB and populates the cache, and new empty nodes also start cold." },
    { q: "Which is a good fit for NoSQL per the classic checklist?", o: ["Double-entry ledger", "Leaderboard / scoring data", "Complex multi-table reporting joins", "Strictly schema-validated invoices"], a: 1, why: "Leaderboards, clickstream ingest, carts, hot tables and lookup tables are classic NoSQL use cases." },
    { q: "A search query fans out to 50 shards and waits for all of them. p99 latency is dominated by…", o: ["The fastest shard", "The slowest shard (tail latency)", "The client's CPU", "DNS"], a: 1, why: "In scatter-gather, the slowest responder sets the latency. Use deadlines, hedged requests and partial results." }
  );
})();
